# 2026-07-04 — 90+ fps arc: the dpr2 cost map

## Mandate (user, refined)
avg 80-90 fps @dpr2 (p50 ≈ 11-12.5 ms), extreme drops bottom 50-60 fps
(17-20 ms). Live today: ~30 fps forest walk / ~50 clearing.

## Instruments
costmap10.mjs (one boot, N poses, gpuWall p50/p95 + median pass table +
counters + within-session grass A-B) · costmap11-scene.mjs (hide each scene
child) · costmap12-px.mjs (bg-null + hide-resolve combos) · spike9-fly.mjs
(moving legs). ⚠️ per-pass spans OVERLAP (sum ≫ wall, twin values) — rank
only, never sum; whole-frame gpuWall A/B is the only ground truth.

## STILL cost map @dpr2 (gpuWall p50, grass-off | grass-on)
- eye (forest walk pose): 23.3 | 27.7    hill: 22.8 | 26.3
- oblique: 33.9 | 37.9                   aerial: 28.9 | 32.0
- Moving (8 m/s walk, yesterday's legs): p50 36.7 → the user's ~30 fps.

## Ladder (whole-frame A/B, eye / oblique, grass-off)
- default 23.3 / 33.9
- postmin=1 17.8 / 30.4 → POST TOTAL ≈ 5.5 / 3.5 (ao 3.7/1.1 the biggest;
  clouds+bounce+contact 2.3/0.6; taa 1.2; bloom 0.8 — overlapping, don't sum)
- pure=1 15.7 / 28.8 → lit-resolve+shadow-sample ≈ 2.1 / 1.6
- nanshadow=0 22.5 / 34 → shadows STILL ≈ 0.8 (strips only re-raster moving)

## DPR scaling law (pure & default, dpr 1/1.42/2 → px 1.49/2.99/5.94M)
Frame ≈ A + B·px, R²≈1. eye default: A≈4.2, B≈3.2 ms/Mpx (pure B≈1.9);
oblique default B≈5.0. dpr1 default eye = 9.0 ms (inside target!). The gap
is per-PIXEL work. ⚠️ visTris also scales with dpr (4.7M→9.5M eye): τ is
device-px-relative (projK ∝ uH), the cut doubles at retina.

## Refuted / null (cheap kills — do not relitigate)
- τ coarsening (?loderr, default 3): loderr=6 halves visTris → only −0.7 ms
  eye; mid values WORSE + unstable (aggregate ladder GROWS geometry when
  coarsening → more overdraw). NOT tri-emit-bound. (First test 1.5/2 was a
  premise bug: default τ=3, lower = FINER.)
- Resolve shading + sky: bg-null + hide naniteResolve + hide naniteResolveVox
  = −0.5 ms total at eye. NOT the slab. (Hiding vox resolve ALONE is +3.5 —
  exposed sky pixels cost MORE than vox resolve.)
- Scene children: no mesh subtree over 0.5 ms (terrain renders THROUGH the
  nanite resolve — TERRAIN-RW).

## THE SLAB: SW raster per-pixel scanline loop (rdbg ladder, world1)
default − rdbg2 (stop before scanline loop): eye 23.3−15.5 = ~7.8 ms;
oblique 34.3−15.9 = ~18.4 ms. rdbg2−rdbg1 ≈ 0 ⇒ per-TRIANGLE work is FREE
(headroom for per-tri tests). Matches the old capture-method finding ("loop
~60% & COVERAGE-bound"). NOTE rdbg legs leave HZB empty (occlusion cull off,
vox raster heavier) ⇒ deltas are LOWER bounds. Vox splat loop NOT rdbg-gated
— its share still inside the 15.5 floor, unknown.

Loop already has: scanline x-span, sample-miss cull, read-before-atomic.
MISSING: any per-triangle occlusion test — cluster-level stale-HZB cull
passes clusters that poke through gaps, then every hidden tri walks its
covered pixels. Lever candidate: per-tri HZB reject (bbox vs stale HZB far),
?scar=1 overdraw counters size the opportunity.

## SCAR overdraw (?scar=1; ⚠️ scar itself +6 ms — global-atomic contention,
## diagnostics only)
eye: totalFrags 6.94M vs 5.94M px = emitted overdraw 1.17× (LOW); band
(leaf) overdraw 9.1×, band share 13%. oblique: totalFrags 6.86M, band share
85%, band overdraw 3.9×. Oblique's loop costs 2.4× eye with FEWER tris and
equal fragments ⇒ the burn is WALKED pixels (sliver tris: bbox ≫ coverage)
+ per-row span machinery, not atomic wins. Occlusion reject would kill
walks of hidden tris — win size unknown, cheap to test (per-tri work FREE).

## MOVING split (8 m/s walk legs, spike9, grass ON)
- default: p50 36.7 p90 40.2 (yesterday, confirmed shape)
- nanshadow=0: p50 28.8 p90 31.0 → SHADOWS-MOVING ≈ 8 ms p50 (still-pose
  0.8!) — the strip re-raster is ~ALL of the moving inflation, even after
  the 5/15 caster warp.
- freeze=1: p50 34.1 → cut churn only ~2.6.
- Still 27.7 vs walking-noshadow 28.8 ⇒ non-shadow moving inflation ≈ +1.1.

## Budget sketch (walking eye ≈ 36.7 → target 11-12.5)
shadows-moving ~8 + tri pixel loop ≥7.8 + post 5.5 + grass 4.4 + lit
resolve 2.1 + floor ~6.6. Workstreams by impact: (1) shadow strip cost
(known machinery: caster τ/budget/cadence/shvox/ungrown lane), (2) per-tri
HZB reject in the SW loop, (3) post (AO 3.7 + clouds/bounce/contact 2.3),
(4) grass ≤2 law (#55). Even all four ≈ 19-20 walking — the 11-12.5 avg
likely ALSO needs the internal-resolution + temporal-upscale lever (dpr1.42
default eye = 14.2 grass-off; dpr1 = 9.0) = USER CALL, surface it.

## W1 session results (shadows-moving ~5-8 ms)
- Fresh same-day walking baseline p50 34.3 (cross-session drift ~2 — chain
  legs same-day only).
- ?shtau=2 → 33.4; =3 → 33.0. Caster-coarseness lever SATURATES at ~−1.3.
- ?shmaxlv=N (NEW knob, generalized P7 strip budget over ALL levels,
  stalest-first, frozen-deferral): N=2 neutral, N=1 worse tails, N=3
  pathological. NULL/NEGATIVE — deferral batches the same strip WORK and
  bloats the shared-cut union box; per-level chain overhead is NOT the slab.
  Default 0 (off). Knob kept as diagnostic.
- Median walking passes: shadow bill = compute +4.7 (cut traverse + 6×filter
  + strip raster, batched — inseparable via tags) + nanClipClear0/1 ~1.5
  (queue serialization). Remaining suspect = the shared-cut DAG traverse
  (runs whole every moving frame; shmaxlv can't touch it). NEXT: per-dispatch
  timing via WebGPU-Inspector capture, or emit per-LEVEL queues from the cut
  (kills the 6× filter re-scan of the 139k-cluster cut output).

## W2 session results (?trihzb per-tri HZB reject — WEAK, default off)
Built: pyramid levels 0-4 mirrored into the world1 hwQueue TAIL (binding
budget: direct pyramid bind = 11th storage buffer = validation death, the
limit really is 10; same fold pattern as scar). Per-tri: finest level whose
2×2 window covers the ≤17px bbox, u32-monotonic f32 compare, frame-0-safe.
- v1 (fixed 32px level): NULL — porous canopy: any gap in a 64px window
  makes farZ=far.
- v2 (levels 0-4, dynamic pick): FIRES — scarTotalFrags 6.94M→6.05M (−13%),
  bandFrags −17% — but frame only ~−0.5 ms eye / noise oblique. VERDICT: only
  13% of tris hide behind SOLID occluders; the loop burn is walking VISIBLE
  sliver tris. Occlusion is not the loop's lever. Kept as ?trihzb=1 opt-in
  (quality unvetted — stale-HZB disocclusion). ⚠️ trihzb+scar combo trips a
  Storage-vs-ReadOnlyStorage render-pipeline validation error (diag-only).

## W5 SHIPPED (commit 7a4c207): ?rscale internal-res + TSR upscale
USER APPROVED the lever; built as ?rscale=S (0.25..1, default 1 = the
unchanged frame; user tests scales themselves, incl. 0.5).
- Architecture: canvas/drawing buffer stays NATIVE. scenePass
  .setResolutionScale(S) + RenderScale.internalSize() (mirrors PassNode's
  floor(logical×pr×S) EXACTLY — vis buffers are fragCoord-indexed, one px of
  drift breaks the resolve). All getDrawingBufferSize sizing call sites
  switched: NaniteFrame/NaniteView (vis buffers, cam, jitter mirror),
  WorldRegistry + ForestScene (τ/voxlod anchorH — the cut coarsens with S,
  intended; anchorH is IN the bootcache key so each S cold-builds once),
  HalfResMrt (0.5×internal), WaterSurface snapshot, RasterSpikeScene.
- TRAA fork TSR mode (gated on S<1, rscale=1 path untouched): history +
  resolve RTs at NATIVE; beauty/depth stay internal; jitter setViewOffset in
  INTERNAL px (incl. the pipeline onBefore hook); prev-depth home moved to an
  own internal-size RT (native history RT can't host the internal copy);
  restart seeds via a _historyValid weight-1 resolve frame (cross-size
  copyTextureToTexture is illegal). PostStack: uInternalSize runiform feeds
  velReproject (it executes in the NATIVE resolve pass — screenSize is wrong
  there); the TRAA input RTT is pinned per-frame to internal (its autoResize
  would run the whole pre-TRAA post chain at native cost).
- MEASURED @dpr2 world, grass-on (costmap10, same-boot-chain):
  eye still 28.9 → 23.1 (S=0.7) → 14.6 (S=0.5); grass-off 24.3 → 15.5 → 9.7.
  oblique 39.2 → 26.8 → 16.5. visTris follows S (9.5M → 6.7M → 4.7M, τ
  device-px). TSR native-res resolve+history overhead is INSIDE these
  numbers (dpr-law predicted 22.4 @0.71 vs measured 23.1).
- Walking (spike9 8 m/s, chain ran THERMALLY HOT — default p50 54.9 vs
  rested 36.7, DVFS booked hazard; ratios only): 54.9 → 38.8 (0.71×) →
  29.3 (0.53×). Rested-scaled estimate: walking @0.5 ≈ 19-22 — the 11-12.5
  mandate still needs the shadow-moving (~8, res-INDEPENDENT) + post + grass
  slabs on top of rscale.
- Stills (shots/rs-eye-{100,070,050}.png, live HUD): 37 → 44 → 72 fps.
  0.7 near-indistinguishable from native; 0.5 visibly softer in grass
  mid-field, zero artifacts/ghosting. Default S TBD by user testing.
- Quality upgrade levers if user wants 0.5 sharper: Catmull-Rom current
  sample, wider variance-clip gamma under upscale, luma-weighted history.

## ARC DECISION POINT (for the user)
Slab work measured so far nets ~2-3 ms of the needed ~24. The still-frame
pure floor (15.7 eye @dpr2) is per-pixel-scaled and survives every cheap
lever tried (τ, occlusion, resolve/sky). Reaching avg 80-90 fps at native
dpr2 by trimming alone is NOT plausible on this evidence. The structural
lever is INTERNAL RESOLUTION ~0.7-0.75 + TEMPORAL UPSCALE (TSR-class; TRAA
already exists as the base) — dpr1.42 whole frame = 14.2 grass-off / 22.4
grass-on BEFORE any slab work. Comparison stills booked (scratchpad
shots/shots/res-eye-dpr{2,142}.png). USER CALL before building.
