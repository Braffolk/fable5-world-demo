# Grass arc — nanite matClass-5 lane (2026-07-03, IN PROGRESS)

Plan: `docs/deep-review/31-grass-plan.md`. Stages S0-S6.
**USER LAWS (both non-negotiable):** (1) no significant perf effect (stated for water+grass,
2026-07-03); (2) **LUSHNESS LAW** (stated with legacy reference shot): the original GroundRing
meadow was EXTREMELY lush/thick — zero density compromise. Perf comes from the DAG ladder /
≥1px law / HZB / election, never from thinning below the ring's tuned coverage
(memory `grass-lushness-law`).

## S0 — resolve branch + debug field (SHIPPED c86a7f2)

- Resolve matClass-5 branch replaces the gray fall-through: GroundRing blade material ported
  (fresh/dry tip ramps × world-anchored ~1.6 m patch dryness hash × canopy shade-darkening;
  blade normal → terrain normal pull hardening with distance; tip-AO on indirect; tip-weighted
  k=0.09 backlight in the shared glow block). ALU + explicit-LOD taps only, zero new storage
  buffers, never built in the vox pass, presence-stripped (RP-1) when class 5 unregistered.
  3-vert bary interp < 30 m, single-vertex cheap path beyond.
- `?grassreg=1`: 6000 bladeClump(5,4) instances, 28 m field at origin, matClass 5.
- **FINDING (structural):** the hier cull seeds ONLY meshes with DAG roots
  (kSeedRoots `returnIf rootCount==0`) — a flat registerMesh is INVISIBLE by construction.
  Every grass mesh must carry a DAG. The debug patch builds its aggregate DAG inline and
  pushes to dagBuilds AFTER the bootcache put, so the shared 'dags' cache never invalidates.
- Verified: blades render with ramp/dryness/shade variation (g1-macro).

## S1 — grass transform channel (SHIPPED 97cf85e)

- makeCtx grass block: gust field taps once per instance; GroundRing response per vertex
  (bend = amp·(s·0.55+0.6)·t²·bladeH·0.42, tips dip, shimmer ⊥ wind). Shimmer sine hoisted
  (per-instance args). TrunkWindFields slots reused — shF broadcast untouched.
- NEW-lane upgrade: shimmer gets the leaf ~120 m fade (ring only zeroed it in far mode).
- Verified: blades bend at wind=1.4 (g2-wind pair).

## NEXT

- **S2 — patch library + aggregate DAG ladder** (R0 5-blade/4-seg clumps → R1 3-blade/2-seg →
  R2 tufts → R3 super-tufts), authored so every rung's tri width ≥ ~1.5 px at its selection
  band (numeric build-time assert at blade HALF-HEIGHT — the tip taper is exempt). Gate:
  single-patch shotdiff vs a GroundRing patch at 5/15/30/60/120 m.
- **S3 — toroidal patch residency + FULL density-law port** (~90 slots/m² near, 5-blade
  overlap, 12 m scruff floor, bank margins, canopy thinning, thin×widen conservation).
  Gate: gpuWall vs grass-free baseline ≤ +2.5 eye-meadow / ≤ +0.5 forest-interior AND
  ≤ the `?grasscontrol` lane net, AND shotdiff lushness parity vs the ring (USER LAW).
- S4 splat handoff · S5 shadows (finest clip only, minPx≥1) · S6 trample field.
