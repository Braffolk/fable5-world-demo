# Microtopography Handoff Prompt - 2026-07-16

You are continuing the `laas` Estonia microtopography task in
`/Users/sebastian/IdeaProjects/fable-demo2` on branch `estonia-asset-gen`.
Read `AGENTS.md`, `docs/specs/terrain/MICROTOPOGRAPHY.md`,
`docs/tasks/2026-07-13/TASKLIST.md`,
`docs/tasks/2026-07-13/KNOWN-ISSUES.md`, and
`docs/tasks/2026-07-13/MICROTOPOGRAPHY-CONFORMANCE-AUDIT.md` before changing
anything. The year is 2026. Use `uv` for Python. Do not push. Make local commits
at clean checkpoints. The user explicitly requires subagents for substantial
parallel tracks, and every subagent prompt must explicitly say: `Do not spawn
subagents.` Subagents must never spawn their own subagents.

## Non-negotiable architecture

- All microtopography is synthesized and packed cook-side in `asset-gen`.
- The browser only streams, decodes, samples, morphs, and renders packed ordinary
  absolute heights. It performs zero terrain synthesis.
- Canonical LOD0 remains 2048 m at 1 m texels. Fine rungs are LOD -1 at 0.25 m
  and LOD -2 at 0.0625 m. Do not reintroduce a global 128 m LOD0.
- Do not change binary formats or runtime ingestion unless a demonstrated blocker
  makes it unavoidable. The current ordinary format-2 path works.
- Exact closure, seams, packing, and tests are necessary but never sufficient.
  Visible beauty, realistic variance, physical material/regime character, and
  meaningful macro plus micro structure are the goal.
- No generic fBm/noise filler, no broad `forest/field/bog/sand` style lookup, no
  national roughness knob, and no scene-specific fixes.
- Source DTM is fallible. Correcting real source errors and making evidence-backed
  bounded macro changes is allowed; perfect up/downsample reconstruction is not
  the objective.
- Do not touch shaders casually. Every shader-line change needs pressure,
  occupancy, register, ALU, and control-flow reasoning. Run exact real-WebGPU boot
  after runtime/shader changes; use GPU traces rather than noisy thermal A/B timing.
- Keep tests lean while systems are changing. Results come before test architecture.
- Give exact-position URLs. Never give compass directions because the UI exposes
  no direction indicator.

## User-owned/unrelated dirty work

Do not revert, stage, or edit unrelated dirty files. In particular:

- `docs/METAL-PROFILING.md` is unrelated user work.
- `asset-gen/src/assetgen/process/micro_synth.py` is unrelated/untracked.
- `.worktrees/`, `docs/deep-research/microtopography-generation/library/data/`,
  `.../datasets/`, `.../repos/`, and `docs/todo-human-only.md` are unrelated.
- `src/render/TerrainMaterial.ts` has been edited separately by the user/other
  agents in this broader session. If it appears dirty later, do not stage or
  revert it unless the user explicitly assigns that work.

Current HEAD is `01cdd60` (`Preserve parked aeolian dune synthesis attempt`). Recent
useful commits are:

- `01cdd60` preserve the terminal parked aeolian-dune synthesis attempt.
- `e9d5d93` pack and independently verify the full LOD0 forest research tranche.
- `8bded0a` ground streamed instances on packed fine terrain.
- `63d82ce` preserve the rejected bog form-scale graph.
- `d060249` record authorized terrain research exceptions.
- `bf0b68b` consume optional geology priors for material appearance.
- `30a16ef` stream optional coarse geology categories.

## Immediate checkpoint: full 2 km forest LOD0 tranche

The locked `forest.mesic_mineral` generator has been run once over complete LOD0
`(0,154,94)`, bbox EPSG:3301 `[684032,6440960,686080,6443008]`, producing one
`32768 x 32768` absolute 6.25 cm master. FLOAT artifact:

`asset-gen/data/work/microtopography/forest-mesic-mineral-lod0-tranche/sha256/6d3bae2fa0ed0da3373deb7aee11c0a044d00c7d3e108aeb79fe37c204c1d53f`

FLOAT facts: 263,799 balanced K11/K32/K36 sites, exact 1 m closure
`6.28e-16 m`, zero hard leakage, residual RMS `0.03410 m`, max `0.45333 m`, and
all six internal parent-seam p95 steps below nearby non-seam p95. Its five QA PNGs
were inspected and accepted for packing.

Packing is now complete and independently verified. Do not rerun it unless a real
defect is found. Recipe:

`e9b45446c7a426bebd6b468d729dcc9f6e555dd3ee6ce7c9b2c20abd62cd2025`

Serveable manifest:

`asset-gen/data/out/builds/e9b45446c7a426bebd6b468d729dcc9f6e555dd3ee6ce7c9b2c20abd62cd2025/m/49d298c42d5d1ac6/manifest.json`

Manifest SHA-256:

`49d298c42d5d1ac600ec3d4e78228177963ca6211a813f08b68caf539b0c12f6`

Verified facts:

- 256 published LOD -2 chunks totaling 504,106,433 bytes.
- 16 LOD -1 parents totaling 39,963,139 bytes.
- 33 transient apron/support chunks.
- Final manifest has 1,097 chunks totaling 1,001,639,616 bytes.
- All seven verification gates pass.
- Fine seams are exactly zero over 544 comparisons.
- Parent seams are exactly zero over all 24 boundaries.
- Maximum decoded LOD0 closure error is `0.00187445 m`, below `0.00501 m`.
- C1 round-trip maximum is `0.00099945 m`.
- Hard-exclusion leakage is zero.
- Changed ancestors are `(1,38,23)`, `(2,9,5)`, `(3,2,1)`, `(4,0,0)`.
- Planned LOD0 keys, including `(0,154,94)`, remain byte-identical because the
  accepted master preserves exact 1 m means.
- Inherited biome, boulders, canopy, debris, soil, trees, understory, water, and
  watercover indexes are byte-identical.
- The pinned source has no literal geology runtime layer. Geology/substrate
  conditioning is baked into the accepted height artifact; inherited soil/biome
  layers remain intact.

The first pack attempt exposed a real shared-parent quantization-offset defect.
The packer was corrected to use one shared parent datum and the entire 289-chunk
fine/apron set plus 16 parents was re-encoded. Never resurrect or publish the first
attempt. The verified recipe above is the corrected second attempt.

Forest implementation files committed in `e9d5d93`:

- `asset-gen/config/microtopography/forest-mesic-mineral-lod0-tranche-154-94-v1.json`
- `asset-gen/src/assetgen/terrain/microtopography/forest_exemplar/lod0_tranche_preview.py`
- `asset-gen/src/assetgen/terrain/microtopography/forest_exemplar/lod0_tranche_preview_verify.py`
- modifications in `asset-gen/src/assetgen/terrain/microtopography/forest_exemplar/adjacent.py`
- minimal recipe/verifier registration in `asset-gen/src/assetgen/release.py`

`git diff --check` passed for these files before the pack. Review their final diff,
but do not start an implementation-review loop or rebuild a large test framework.

## Immediate next action: finish forest browser gate

Local servers were started and may still be alive:

- Vite: `http://localhost:5193`
- Immutable data preview: `http://localhost:8803`

The data server was launched with:

```sh
PORT=8803 \
LAAS_PREVIEW_MANIFEST=builds/e9b45446c7a426bebd6b468d729dcc9f6e555dd3ee6ce7c9b2c20abd62cd2025/m/49d298c42d5d1ac6/manifest.json \
node tools/serve-data.mjs
```

If the servers are dead, restart them on fresh ports rather than assuming. Three
of four exact real-Chromium/WebGPU boots passed with no page, HTTP, stream, TSL,
WebGPU, pipeline, or bind-group errors:

1. Dense floor, frame 94:
   `http://localhost:5193/?scene=world&src=estonia&dataurl=http%3A%2F%2Flocalhost%3A8803&alt=12&x=316752&z=193296&yaw=0&pitch=-0.22&shadowclipres=896`
2. Eligibility transition, frame 93:
   `http://localhost:5193/?scene=world&src=estonia&dataurl=http%3A%2F%2Flocalhost%3A8803&alt=12&x=316656&z=193136&yaw=0&pitch=-0.22&shadowclipres=896`
3. Higher base relief, frame 96:
   `http://localhost:5193/?scene=world&src=estonia&dataurl=http%3A%2F%2Flocalhost%3A8803&alt=12&x=316208&z=194032&yaw=0&pitch=-0.22&shadowclipres=896`

One boot remains:

4. Strong added morphology:
   `http://localhost:5193/?scene=world&src=estonia&dataurl=http%3A%2F%2Flocalhost%3A8803&alt=12&x=316880&z=193392&yaw=0&pitch=-0.22&shadowclipres=896`

Run it with `npx tsx tools/boot-smoke.ts` and a labeled screenshot. The very first
dense-floor boot failed once with a transient Three/TSL `null.constructor` console
error, but an immediate identical immutable retry passed at frame 94. Record the
one transient honestly; if it repeats, diagnose it before user exposure. Do not
hand the user a link that has not actually booted.

After the fourth boot passes, give the user the four exact links and pause for visual
review. Ask them to inspect natural variation/repetition across the full 2 km extent,
absence of storage or eligibility seams, and agreement of terrain with trees,
understory, debris, grass, and materials. Do not give movement directions. The user
previously accepted smaller forest pilots as natural and good; this checkpoint is
specifically the complete-L0 scale-out/repetition/transition/grounding gate.

Then update `TASKLIST.md` and `KNOWN-ISSUES.md` with the verified recipe, manifest,
cost, boot frames, transient boot note, and pending or accepted user verdict. The
generic streamed-instance grounding gap described in the older conformance audit
was fixed by commit `8bded0a`; update stale wording. Commit only the forest files and
task records. Do not stage unrelated dirty files. No push.

## Completed next-regime attempt: aeolian dunes

The first bounded `sand.dune_aeolian` FLOAT attempt is complete and must be PARKED,
not packed. Isolated implementation package:

`asset-gen/src/assetgen/terrain/microtopography/sand_dune_aeolian/`

Artifact:

`asset-gen/data/work/microtopography/sand-dune-aeolian/float/sha256/4edad71d4943fee31e99394f9c76dd01c18f29b61bdf6318a425f405152cea45/`

The pilot was selected from official EGT coastal aeolian sand FID 4659, fully
mapped forest, no hard/protected cells, centered at EPSG:3301
`394368,6533632`. It uses official Maa-amet DTM sheet `61391`, SHA prefix
`d555efe1...`, and only the qualified Mrzezyno `>=1 m` morphology prior artifact
`f6aa61f6ecd256d04175d9a9bf72809d488383e3695bd844eb388ff2bf829e9a`.

Inspect these four PNGs one-by-one, not as an unlabeled dump:

- `qa/01-whole-organization.png`
- `qa/02-native-common-light-before-after.png`
- `qa/03-scale-bands-and-orientation.png`
- `qa/04-masks-and-closure.png`

Mechanical results pass: closure maximum `7.1e-15 m` and shore alignment `0.998`.
The beauty/capacity result fails: residual p95 is only `3.8 mm`, and native
common-light before/after is visibly indistinguishable. The cause is structural,
not a knob problem. Exact 1 m block-mean authority removes the only evidence-backed
1-8 m dune relief, while Mrzezyno does not authorize independent sub-metre geometry;
local gradient matching correctly refuses to amplify a flat Estonian parent to the
foreign source's dune relief. Do not tune amplitude, relax masks, invent ripples,
pack it, or send it to browser.

Resume dunes only if one of these materially new conditions is met:

1. The architecture explicitly permits bounded evidence-backed correction of the
   1 m parent within mapped aeolian dunes before hierarchy reduction.
2. A qualified Baltic exemplar retains more than 1 cm credible residual after exact
   1 m projection.

The four PNGs and manifest/verdict were inspected, and the isolated negative-result
implementation is preserved in commit `01cdd60`. Its task-record update is part of
the subsequent ledger/handoff checkpoint. Do not pack or reopen it without one of
the materially new conditions above.

## Regime priorities after forest handoff

Keep the ranked table in `TASKLIST.md` active and explicit. Current broad state:

- Highest importance: lithology-specific sandstone/carbonate outcrops, erodible
  banks, cliff shoulders/faces/toes, colluvial/talus structures. Structural source
  repair exists, but no accepted morphology owner exists. Three chain/strip attempts
  are exhausted and parked. Resume only with materially new long-context,
  material-matched evidence or a different representation. Cliff work must include
  macro structure at roughly 0.5-1 m and micro detail, not merely bumpier faces.
- Ordinary mesic-mineral forest: current usable research owner; more than halfway
  done. Full-L0 browser/user verdict is the immediate remaining forest checkpoint.
  Pit/mound and managed/clearcut forest remain separate future owners.
- Raised bog: attempted form-scale graph is parked. It creates evenly spaced contour
  corrugations and isolated pool sockets. Resume only with topology that branches,
  merges, terminates, and couples pool margins.
- Aeolian dune: the one bounded attempt above is parked for structural evidence-limit
  reasons. Beach sand is a separate regime and remains unsupported.
- Agriculture: not done and lower visual priority than cliffs, peat, and forest, but
  cannot be silently omitted. Prior corduroy/lattice attempt is rejected.
- Grassland/pasture/meadow/turf, alvar/karst, shores by substrate, fluvial/wetland,
  glacial till/esker/outwash, and technogenic ground remain distinct incomplete rows.

Do not return to random boulder experiments. Small family-4 stone sockets are already
accepted as subtle forest continuity only; boulder-scale macroform remains parked.

## Work style

- Keep one coherent owner per substantial track and use parallel agents only for
  independent result-producing work.
- Do not repeat tiny implementation/review cycles. Let one agent complete a bounded
  conversion or synthesis checkpoint, then review it once.
- Parallel-screen candidate datasets and advance the first usable ones. Preserve code
  for difficult sources but drop focus quickly rather than burning time forcing them.
- Limit a track to two real failed mechanisms before parking it unless the user
  explicitly reopens it.
- Produce a small number of relevant, clearly labeled PNGs when visual evidence matters.
- Update the task ledger after each terminal result so context compaction cannot erase
  the work state.
- The next agent should finish the fourth forest boot, provide the four URLs, and pause
  for the user's visual verdict before starting another large task.
