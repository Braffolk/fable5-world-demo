# Microtopography Task List

Last updated: 2026-07-16

## Objective

Ship beautiful, physically credible, materially and geomorphologically conditioned cook-side microtopography across Estonia. The browser only streams and renders packed height data; it never synthesizes terrain detail. Suur Taevaskoda is a fixed visual reference for generalizable improvements, not a scene-specific target.

## Current State

### Active 2026-07-15 checkpoint

- [x] Complete immutable Taevaskoda structural transaction `009fd3d2be07d465199e6330ac9d2fc5a8fc7a16bbb5df0f55e5030ce962d3d1` steps 01-07: authority, scientific closure, bounded structural materialization, corrected base and water transactions, staging, and corrected format-1 base release.
- [x] Preserve prior transaction `7a2d765354c362bbe15fd0c7eac2c27425123dc3f6c76692d1902618936c1a2b` as superseded immutable history; it is not the active preview transaction.
- [x] Fix the staged-plan LAC1/format-2 validator defect and the later verifier-only quantization and snapshot-comparison defects. These fixes do not replace the completed `009fd...` structural artifacts or step markers.
- [x] Complete independent preview verification for exact transaction `009fd...`: `releaseReady:true`, preview recipe `74fdccda596b124342a8cb956968fdc3339bf56a2bfb8791f58c73cb0f5d0488`, exact manifest SHA-256 `6c7c108fe119990fe8518c81f79294e9b33b73a8723e4f7d92e1efd9f37a6986`.
- [x] Serve the verified manifest from the fresh `http://localhost:8788` endpoint and pass the mandatory real-Chromium WebGPU boot through frame 106 at the exact centered Estonia URL: `http://localhost:5173/?scene=world&src=estonia&dataurl=http%3A%2F%2Flocalhost%3A8788&alt=40&x=311123.082&z=190723.435&yaw=0&pitch=-0.18&shadowclipres=896`.
- [x] Complete user visual acceptance of the Stage 1 corrected-source result at exact URL `http://localhost:5173/?scene=world&src=estonia&dataurl=http%3A%2F%2Flocalhost%3A8788&alt=40&x=311123.082&z=190723.435&yaw=0&pitch=-0.18&shadowclipres=896`, preview recipe `74fdccda596b124342a8cb956968fdc3339bf56a2bfb8791f58c73cb0f5d0488`, and manifest SHA-256 `6c7c108fe119990fe8518c81f79294e9b33b73a8723e4f7d92e1efd9f37a6986`. The user confirmed the river anomalies are gone and the result looks correct; this accepts structural repair, not a morphology specialist.
- [x] Complete the content-addressed Taevaskoda RGB/CIR/ETAK orthophoto QA artifact; it remains conditioning/review evidence, never metric height truth.
- [x] Retain the selected Hovi shared records, ten HY_SPRUCE4 photos, and merged geometry; complete condition evidence, raw-support QA, semantic-photo QA, and final code-bound Q1 candidate `1e5f674af3fc...`.
- [x] Record the independent Q1 `GO` verdict narrowly: it authorizes retention and audit of the HY_SPRUCE4 full scans, not a terrain surface or synthesis. The candidate remains `raw_candidate`, unqualified, unusable as target truth, and has an empty authorized-surface set.
- [x] Retain and exact-verify `HY_SPRUCE4-full.zip` (`47,345,435,519` bytes) under transaction `cabb67ce94a2...`; its safe archive inventory contains exactly one member. Separately retain and evaluate only the downsampled HY_PINE2 LAZ as a same-site stress condition; do not fetch its 46.7 GB full archive.
- [x] Authorize and implement the bounded HY_SPRUCE4 full-archive metadata-extraction path without authorizing a terrain surface, analogue transfer, or synthesis.
- [x] Complete the authorized restart-only HY_SPRUCE4 extraction after Stage 1 verification: immutable object `7889a9cfef69d57a50eaa5a9f8d224d28486d5901163e08cbdc827f92f409da2.e57` is exactly `84,817,645,568` bytes and is bound by transaction `fe44c5c05c4...`; synthesis remains unauthorized.
- [x] Select the corrected receipt-gated HY_SPRUCE4 metadata inventory `54e21049ac09c7d3f69ac7ab5990c982d12a7bd04da0618de032c8311cd3cfad` through frozen authority `config/evidence/hovi-hy-spruce4-full-metadata-selection.json` (SHA-256 `bc47f334c71cf26bd9fd090301d8848ca7b6c37bb4efedc430c3cb2fbd59d360`). It preserves present-empty numeric lexemes with effective zero, reads only the header and descriptor-bound XML, decodes zero records, binds the loaded Python implementation separately from source snapshots, and supersedes `f446b813e4d5...`, `3a93b4db69c3...`, and `456be8611194...`; no artifact authorizes point conversion, a target surface, or synthesis.
- [x] Implement and freeze the bounded native Hovi E57 reader boundary through local commits `7f520b9`, `191dc74`, `50afde3`, `ae23e46`, `2344e36`, `4c6e558`, and `87ac239`. The last three accept equivalent quaternion signs, mask low-bit integer codes before domain validation, and bind the corrected codec build respectively.
- [x] Complete and publish the bounded native reader probe under exact execution selection `183f0466548173c9053e120b1df72465648536cbf3bfadfb20dc22f8dc4709f1`: `65,536` records, exactly `6,291,456` canonical bytes, record SHA-256 `434b584e90575eecfbba5431be95f07e222fb78468ffe0a579d641229ea9015f`, publication-report SHA-256 `70bcc1555fb74683a6caa9e67eec35e5535ccfe4a47dec0e8210523f7a244cbf`, and `0.863 s` elapsed. This is reader validation only: it does not reach EOF, validate publisher counts, qualify a surface, or authorize synthesis.
- [x] Freeze and independently audit the full-scan materialization boundary through native commits `7dfed50`, `e9eb37a`, Python verification/provenance commits `e77bbc9`, `6195439`, `999724d`, candidate-reader commit `aad4941`, and launch commits `4c092ce`, `b37e865`. The final boundary caps EOF integer decoding by the declared record remainder, proves cross-artifact source-ordinal disjointness with one bounded per-scan bitmap, binds the realized NumPy distribution, and carries one absolute decode/verification/publication deadline.
- [x] Correct and independently audit terminal codec-word padding through native commit `ba92ab4`, bind execution selection `1b62c825a75d7092b61bef6be8dcfdd98f55023178ac45fb2d6938cb995bf4ab`, and complete the 16-scan pass. Hash-bound manifest `ed02d20970b5d9eedf5b36406c947d3cea87c59682bc8853f1a23fac23f35d18` records all `4,248,797,321` publisher records decoded and `3,680,759,265` AOI records in 1,907 artifacts (`103,061,259,420` bytes). The embedded native report correctly describes its staging-only child boundary; the Python publisher subsequently atomically moved the verified tree to the final content-addressed materialization path. This closes decoding, materialization, and publication, not scientific qualification.
- [x] Complete the final HY_SPRUCE4 full-scan candidate checkpoint and park it with scientific `NO-GO` decision `hovi-hy-spruce4-full-candidate-no-go-v1`. Central 6.25 cm QA index `261e2e9999ae...` has all 4,096 cells observed but only 234 sole raw candidates, versus 2,550 competing and 1,312 unresolved/overflow cells. Full-versus-thinned product manifest `2a221fde27ed...` conserves `3,680,759,265`/`714,456,347` full/thinned points; at 6.25 cm it finds 448,858/448,850 occupied cells but a median thinned/full point-count ratio of `0.2820`. Preserve the valid conversion and labeled PNGs, but do not tune thresholds or promote this evidence to surface, target truth, analogue transfer, or synthesis.
- [x] Retain the exact 5,773-byte Evo 2024 plot-selector CSV under retention `1584bd396efe...` and materialize corrected plot-1086 condition evidence `73b39fd621fa...`; it remains unqualified, abstaining, and not synthesis-authorized.
- [x] Complete the one-pass conformance audit against the normative spec and current dataset outcomes in `MICROTOPOGRAPHY-CONFORMANCE-AUDIT.md`; preserve the exact boundary between accepted structural repair, unqualified conversion evidence, preregistration exposure, and morphology release authorization.

### Ordered remaining work

- [x] Freeze the separate full-read authorization, exact 28-byte ABI, exact integer support-AOI ticks, per-scan 4 m shard layout, 120 GiB output ceiling, 216 GiB launch gate, and 96 GiB live reserve; bind them to the audited native/Python execution identities.
- [x] Bound every integer unpack by the declared remaining record budget. Codec-word commit `ba92ab4` now requires the exact E57Ref terminal zero-padding length after that budget; legal padding may be at least one narrow code wide because declared `recordCount`, not padding decodability, owns termination.
- [x] Run three lean dataset tracks in parallel through inspectable candidate evidence and labeled PNGs: (A) HY_SPRUCE4 full-scan materialization, (B) Evo 2024 plots 1086 and 1065, and (C) HY_PINE2 downsampled LAZ. Preserve the parked Evo/HY_PINE2 code and provenance; neither sparse or abstaining result authorizes synthesis.
- [x] Atomically publish the verified HY_SPRUCE4 one-pass materialization without changing its evidence boundary. Manifest `ed02d209...` embeds the native child's `atomicPublication:false` staging report; the Python parent verified and atomically moved that staging tree to the final content-addressed materialization path. Publication preserves the unfiltered raw-candidate status and implies no ground filtering, surface claim, target truth, analogue qualification, or synthesis authorization.
- [x] Fetch and verify the exact `889,454,592`-byte Evo plot-1086 LAZ (`113d5d062240...`) under retention `ee5767e0133...`, recover all `302,050,000` records from its malformed zero-count/chunk-table header without changing source bytes, and publish 6.25 cm raw candidate build `9a5598c601d2...`. Its implementation-bound NPZ, inventory, and three labeled PNGs preserve all-return support, `treeid=0` unassigned-return support/envelopes, and the source `z-h` reference while keeping substrate, floor semantics, target truth, and synthesis authorization explicitly unresolved.
- [x] Reuse the same exact acquisition and sequential-recovery path for the only other frozen qualifying Evo plot: retain plot 1065 LAZ (`1,148,190,720` bytes, SHA-256 `7fc725b4...`) under `c4cb8659e522...` and publish raw candidate `4d41d72a7c19...`. It recovers `394,450,000` records, places `296,803,717` inside the official 32 m square, and improves 6.25 cm all-return/`treeid=0` occupancy to `99.7467%`/`97.6994%` versus 1086's `98.2819%`/`93.2961%`. This is better raw support only; surface semantics, target truth, transfer, and synthesis remain unauthorized.
- [x] Freeze and run one direct Evo floor-sheet estimator without plot-specific tuning: exact `h=0`, `treeid=0`, last returns; a 2 cm robust local-plane refit; three source-ID groups as a redundancy gate; four-quadrant center bracketing; no interpolation. Plot 1086 build `bba7a00ad160...` and exact unchanged 1065 transfer `674116851f1f...` retain `55.0%`/`28.2%` and `59.7%`/`34.0%` direct/geometrically valid cells respectively. Preserve both as sparse converted candidates and park them from target/synthesis use because most cells remain unsupported, source IDs cannot be mapped to independent physical views, and stable-organic/deadwood/low-vegetation semantics plus calibrated error remain unresolved.
- [x] Retain only the authorized `351,166,481`-byte Biała Góra first epoch (`2022-02-27.zip`, MD5 `c7a0df4c...`, local SHA-256 `d14fb400...`), verify/extract its sole `605,800,031`-byte LAS without changing the archive, and publish 0.5 m calibration build `4fdbaa3f0a9c...`. The 17,817,615-point RGB/return/class-2 artifact and three labeled PNGs clearly expose coastal cliff, gully, scarp, beach-transition, and mass-wasting/process morphology plus heightfield-risk zones; they do not establish generic till transfer, authoritative ground, independent error, 6.25 cm support, Estonia transfer, or synthesis authorization.
- [x] Selectively retain and exact-verify the official `48,058,366`-byte FORWARD Marrviken harvest-area DTM (MD5 `421586d0...`, SHA-256 `592ce462...`) without fetching the 1.07 TiB release, and publish unqualified candidate build `d33ae4bbc122...`. Its 0.25 m provider grid and three labeled PNGs expose 273 interior 32 m patches, fixed 0.10/0.50/0.90/0.99 roughness-quantile samples, and explicit raster-edge exclusion. It supports boulder/rough-forest inspection only: the DTM predates machine tests, lacks measured-versus-kriged support, is one-site provider processing rather than independent truth, and does not authorize synthesis or Estonia transfer.
- [x] Fetch and convert the exact 1,775,923,667-byte HY_PINE2 downsampled LAZ through raw-support and candidate-sheet evidence with PNG QA. Its frozen sheet correctly abstains at `49.61%` direct support and `20.03%` largest-component coverage; it is parked as a same-site/campaign stress condition, never independent validation or a synthesis target.
- [x] Convert the HY_SPRUCE4 materialization into raw view-aware sheet hypotheses with scan-balanced evidence, unique-view/angular descriptors, descriptive inter-scan disagreement, raw full-versus-thinned occupancy/proximity comparison, explicit semantic/visibility/error unknowns, and five labeled PNGs. Decision record `config/evidence/hovi-hy-spruce4-full-candidate-decision.json` parks the track because the fully observed central field is dominated by competing or unresolved sheets; these diagnostics are not a candidate terrain surface, transfer proof, or synthesis authorization.
- [x] Complete the bounded HY_SPRUCE4 semantic/support/error feasibility probe and obey its stop. Semantic binding `39a6ba366015...` preserves four publisher quadrat compositions as plot context but, without metric photo-to-TLS registration, authorizes zero cell labels. Final build `13bb3582f225...` records `target_evidence_insufficient` for B1 and B2 because the accepted semantic surface, independent XY/Z control, held-out same-surface reconstructions, and acquisition transfer are missing. This is a conformant no-go, not synthesis authorization.
- [x] Complete protocol step 1 for ForestSemantic-MS under build `aab4b9b6812d...`: retain and verify all six official LAZ files (`131,104,665` bytes, `9,600,927` labeled points), preserve the publisher train1-4/test1-2 split, and publish three semantic QA PNGs. It is a bounded `GO` for ground/low-vegetation/woody-debris classifier development and audit only; absent CRS/view/return metadata, sparse test woody debris, and missing independent positional/annotation error block height truth, Hovi validation, and Estonia transfer.
- [x] Pre-screen three replacement zero-budget target sources in contract `replacement-target-selection.2026-07-14.json`. StREAM and Point Beach fail their independent-control/error gates. Exact Mrzezyno `2022-02.zip` is a bounded metadata `PASS` only for a dry Baltic dune/beach/scarp B1 support/error probe; it does not authorize B2, fine ripples, water, generic terrain, Estonia ownership, or synthesis.
- [x] Retain and convert exact Mrzezyno `2022-02.zip`, then run the frozen bounded B1 probe as build `101d71b16d90...`. It explicitly invalidates three undeclared extreme cells and leaves `7,799,605` IDW cells support-unknown; six height-blind contexts show descriptive B1 relief, but absent original points, per-cell support, dry-sand semantics, and band-resolved error leave zero authorized cells. Park the site as `no_go_as_b1_target_site`; do not reinterpret its provider DEM as target truth.
- [x] Pre-screen the next bounded replacement tranche in `replacement-target-selection-2.2026-07-14.json`. Reject AHN4+BGT because 10-14 points/m2 and its published systematic/stochastic errors cannot resolve the B1 lower edge. Advance TU Dresden OPARA-1038 only as a bounded bare cultivated-loess candidate: raw imagery/dense geometry, explicit soil/tillage/rainfall protocols, CC BY 4.0, millimetric support, and held-out controls are present.
- [x] Complete capped independent complement searches without geometry downloads. `agriculture-complement-selection.2026-07-14.json` rejects Poznan (process metadata only; no geometry/control) and Evora (derived orchard surface; mismatched semantics and incomplete error/support). `peat-rock-target-selection.2026-07-14.json` rejects HovoPhoto40 and Wards Hill because their millimetric geometry/control cannot be joined to machine-usable surface/exclusion masks. Do not reopen these tracks without the recorded missing primary evidence.
- [x] Freeze the exact OPARA 2021-07-21 pre-rain sample-plot event and retain only `00_B_sample_plot.7z` (`3,307,259,536` bytes). Candidate `f6714a2f98a0...` is a terminal pre-geometry no-go: dense epoch 0 is absent, 228 images cannot bind to it, nine controls have no GCP/CP roles, raw event logs are absent, the global log excludes the event and has no axis-specific CP residuals, and reported vegetation/rock cover has no mask. Preserve the exact archive/inventory; do not treat point precision as independent truth.
- [x] Run the metadata-only reauthorization audit for exact OPARA `00_A_sample_slope.7z` (`4,597,207,850` bytes). Contract `opara-1038-sample-slope-metadata-audit.2026-07-14.json` rejects it with a zero-byte ceiling because the official bitstream promises no named vegetation-free event, event-specific raw/processed pairing, held-out axis residuals, same-epoch TLS/UAV identities, or machine-usable masks. The archive was not downloaded; parent archives remain forbidden.
- [x] Complete the capped disjoint small-surface tranche without geometry downloads. `agricultural-small-surface-selection.2026-07-14.json`, `calibrated-multiview-surface-selection.2026-07-14.json`, and `small-europe-field-surface-search.2026-07-14.json` find no source that jointly supplies reusable geometry, explicit surface semantics, raw/reconstructable support, and independent control. Preserve exact resume conditions; do not continue an unbounded dataset hunt.
- [x] Obtain user approval and integrate the bounded weak-evidence research path. The forest-amendment checkpoint spec SHA-256 was `bbdf34b222f2...`; the current erodible-slope amendment hash is bound in its machine preregistration. Research artifacts may reach immutable non-`latest` browser previews but gain no production target, owner, transfer, or publication authority.
- [x] Freeze and run forest design `forest-floor-research-design.2026-07-14.json` (SHA-256 `b2f040d096aa...`). Repaired immutable artifact `315579b48189...`, manifest SHA-256 `87cd36250b2e...`, preserves the one-shot result: train4 calibration passed, `test2` passed, but untouched `test1` forbidden-area UCB95 was `0.2023217` against `0.02`. The exact ForestSemantic-to-Hovi recipe is `NO_GO_before_model_training`; no Hovi rescan, model training, synthesis, Järvselja opening, or Taevaskoda tuning occurred. Resume forest through materially new evidence with a new untouched audit, not by retuning consumed test1/test2.
- [x] Select and freeze Moore M0 as the next explicit-surface route in `moore-m0-research-design.2026-07-15.json` (SHA-256 `99290f3ac260...`): 68 vegetation-clipped visible-moss grids, eight geographic groups, `309.1387 m2`, four exact phases, joint B1 plus `derived_B2_hypothesis`, and D/R/E/corrected-only candidates. B2 must pass the frozen source and held-out synthesis gates; a B1-only preview is forbidden.
- [x] Implement and run `moore-m0-materialization-v1`. Build `5bdc602d32bc...`, manifest SHA-256 `13b7a18304ae...`, closes all 78 artifacts and reproduces 68 grids, 3,091,387 cells, `309.1387 m2`, and six groups with common B2 support. The terminal result is `target_evidence_insufficient_for_moore_decimeter_research_preview`: strict B1 cells, accepted windows, spectral cores, and persistent form families are all zero. D/R/E and any B1-only preview remain unauthorized; preserve the route rather than padding small plots.
- [x] Run the bounded OPARA later-epoch feasibility check against exact retained `dense_438`. The final retained cloud has `2,260,817` vertices over only about `3.050 x 2.351 m` (`2.050 x 1.351 m` after a 0.5 m boundary exclusion), constant class zero, reconstruction confidence rather than semantic/view identity, and no defensible masks for the documented vegetation/rock mixture, wet surface, frame/targets, occlusion, or interpolation. It is terminal `NO-GO` as R1 weak supervision and remains only an R0 cultivated-loess erosion diagnostic; do not reinterpret its precision or M3C2-to-missing-epoch-0 companion as truth.
- [x] Run one capped, parallel neighboring-country activation pass for immediately usable peat/rock and cultivated-soil sources. No candidate clears production or R1 target gates. Preserve Stordalen Mire's exact 2019 5 cm CC BY 4.0 point cloud/DSM plus later multi-epoch series as the only source worth a bounded B1+B2 process-hypothesis checkpoint; preserve SITES Robacksdalen as one cheap agricultural metadata lead; park Kotkanoja behind unavailable geometry and Getaberget behind imagery-only payloads.
- [x] Run the Stordalen multi-epoch source checkpoint before amending the spec or synthesizing. It fails the first conjunct: the official SITES graph exposes a 2018 DSM, one 2019 PC+DSM+orthomosaic acquisition, and one 2024 DSM+orthomosaic acquisition, not three paired raw-point/DSM epochs. Available station-scale land-cover maps are not aligned 5 cm water/tall-vegetation/support masks, and no independent surveyed control is published. Terminal result is `NO_GO_as_B1_B2_process_hypothesis`; spend zero bytes on the 1,058,256,396-byte 2019 format-only PC+DSM download.
- [x] Materialize the one permitted OPARA R0 negative-form diagnostic as artifact `441eaf9194cd...`. Three fixed support/mask/baseline variants retain 13 connected, downslope-aligned depressions with lengths `0.088-0.571 m`, equivalent widths `0.014-0.049 m`, and median depths `0.0071-0.0139 m`, plus four labeled QA PNGs. Every measured width is below the 0.125 m wavelength representable by the 0.0625 m renderer grid, so the artifact authorizes only qualitative evidence that connected incision/deposition processes exist. Do not widen, upscale, copy, or parameter-transfer its geometry into synthesis.
- [x] Build one bounded development prototype for `agriculture.cultivated` as an R0 process hypothesis on exact Estonia LOD -2 tile `(2426,1498)`. Artifact `de1f0d4adb49...` emits an absolute 2049-square surface and eight labeled PNGs, but the completed end review rejects it as morphology: per-tile hydrology changes the shared east edge by up to `0.0112528 m`; fixed 0.30 m rows own 94.998% of samples and visibly form corduroy; clods are repeated analytic lattice stamps; parcel/mask/soil claims are not source-artifact-bound; and the displayed B1/B2 panels use non-normative box/zoom operators. Preserve this exact result as failed development evidence, never synthesis authority.
- [x] Park agriculture before browser authorization/cook. The user explicitly deprioritized it as the first visual showcase, and the independent review found hard geometry failures. Retain its useful lattice/base/typed-contribution/QA scaffolding to avoid reimplementation, but transfer no morphology parameters and spend no correction cycle on this route now.
- [x] Select the next visually rich, generalizable morphology regime from already-audited evidence: `fluvial.rill_gully_seep_spring` on dry, single-valued erodible mineral bank/slope surfaces, with sandstone-derived substrate as a condition and compatible colluvial toes as a transition. It owns connected runoff incision, shallow headcuts, seep relief, and deposition, not cliff faces. Vertical walls, caves, undercuts, detached blocks, talus requiring multiple heights, and water remain excluded. This is the best visible-value/heightfield-risk tradeoff and improves physically matching sites across Estonia without a Taevaskoda coordinate rule.
- [x] Amend the accepted spec to authorize `erodible-slope-research-bundle/1` for bounded R0 development and a later immutable non-`latest` preview, then add the machine preregistration scaffold at `asset-gen/config/microtopography/erodible-slope/bundle-preregistration-v1.json`. Preserve its initial A/B/Orajõgi roles only as historical preregistration input; the frozen replacement authority below supersedes B as a positive Development role and Orajõgi as sealed OOD.
- [x] Materialize the provenance-clean A/B condition bundle `77b81b2dc051...` (bundle file SHA-256 `0637109bf0b8...`) with exact accepted corrected/canonical LOD0 height, fine structural masks, ETAK source geometries/distances, parsed soil profiles, decoded EGT 1:200k geology, real outlets, conservative evidence domains, independent `+128 m` enlargement grids, hard masks, localized topographic seep-hypothesis support, and eight labeled QA PNGs. It is ready for non-authorizing R0 input freeze only; it does not qualify replacement Development C or authorize recipe/preview freeze.
- [x] Enforce cellwise material abstention in the condition bundle. Development A has parsed Loimis1+Huumus over all `235/235` target cells on normal and enlarged grids and is the morphology-development input. Development B has `0/801` supported target cells because missing Huumus is unknown, not absence; preserve it only as a strict abstention/negative development record.
- [x] Preserve the initial replacement selection `15d76d27aade...` and condition artifact `fa43f7075214...` as superseded history. Its ETAK `9688719` crop `(2438,1519)` is rejected condition-abstention evidence rather than a runnable morphology site after the frozen condition-relevance audit selected the adjacent canonical crop. Orajõgi `9688685` remains consumed/disqualified, and OOD2 `9688702` remains sealed, uninspected, and bounded-abstention-only.
- [x] Freeze corrected Development C selection `aa59aca7a520...` (selection file SHA-256 `977b336b65c7...`) at ETAK `9688719`, representative point `E680752.8418668837 N6441183.486937005`, exact LOD -2 chunk `(2438,1518)`, and bounds `[680704,6441088,680832,6441216]`. The selection's support crop is required to equal the exact output chunk; stale crop metadata fails materialization.
- [x] Materialize and reconstruct the superseding A/new-C/B condition artifact `10a5ea53b007...` (bundle file SHA-256 `9d417818c90b...`) from exact normal and independent `+128 m` evidence windows. A and new C are the only runnable morphology inputs; B remains `0/801` strict-abstention evidence and old C remains rejected history. New C's exact crop matches both windows and the frozen selection: `16,384` cells, `9,713` solve cells, `13,346` material-supported cells, `3,038` unknown cells, `122` target/solve/material-supported cells, `100.15510404768838 m` exact target-line intersection, and zero edge leaks. Twelve compact labeled QA PNGs remain indexed with the bundle.
- [x] Run and reject candidate `90df9d5cbb36...`. Its safety/invariance accounting passes, but Development C fails visible morphology: all `122/122` mapped target cells are protected breakline cells, none is form-active, and only sparse neighboring forms survive. Preserve `90df`, `aa59...`, and `10a5...` as historical evidence; this is a condition-selection failure, not a solver failure.
- [x] Replace target-line counting with input-only typed process-opportunity qualification recipe `07d0e5d40ee4...` (proposal file SHA-256 `29d902e5ac2a...`). It applies actual hard/collar/form-active semantics, bound runoff-form eligibility and separate seep support, connected extent/contact gates, and normal/enlarged identity. Neither local Development C chunk nor metadata candidate ETAK `9688758` qualifies; Development A remains the positive control and sole runnable morphology-development site.
- [x] Complete and inventory the bounded national two-sheet acquisition as exactly six retained Maa-amet products. Sheet `63944`: 1 m DTM `76,325,854` bytes, SHA-256 `0a38695b75af...`, HTTP provenance `4d764b660d77...`; RGB 2025-05-10 archive `208,852,782` bytes, SHA-256 `db494371d8f8...`, retained identity `00d4b3b49ad3...`, extraction inventory `2b9af317ad5d...`; CIR 2024-04-30 archive `190,872,479` bytes, SHA-256 `845f78026b88...`, retained identity `ee5502e53f5e...`, extraction inventory `ad02a3116167...`. Sheet `65901`: 1 m DTM `64,823,889` bytes, SHA-256 `5b675e00e25b...`, HTTP provenance `3fe62a5df7d3...`; RGB 2022-05-25 archive `118,558,922` bytes, SHA-256 `51d592eba3e0...`, retained identity `3974b6a179fb...`, extraction inventory `958e667b9762...`; CIR 2022-05-25 archive `112,415,227` bytes, SHA-256 `72a10cdb0ed4...`, retained identity `34d60d0e262f...`, extraction inventory `8209957cb27f...`. No imagery pixels were opened for candidate ranking.
- [x] Exhaust the frozen national candidate order under corrected opportunity recipe `07d0e5...` and publish bounded no-pass report `66d3457fbf03...` (file SHA-256 `fbe4bf61f5fc...`). ETAK `1812065` has a `1,069`-cell, `105.119 m` seep component but only `7` target-contact cells against the frozen minimum `16`; `1812320` peaks at `123` cells, `18.439 m`, zero contact; `1812321` peaks at `307` cells, `28.601 m`, zero contact. Normal and independent `+128 m` masks are identical for every evaluated chunk, all candidates have zero active materialization blockers, and OOD2 `9688702` remained source-excluded and unopened. Park further site search rather than weakening any gate.
- [x] Run the bounded correlated Development A west/east R0 evaluation as artifact `61485b0da781...` (manifest SHA-256 `8f5756c0cb4e...`) from one shared whole-domain baseline solve/form plan, one seep-off control, and one independently enlarged-domain control. The 2,049-sample shared edge is exact for C0, C1, residual, incision, deposition, ownership, hard exclusion, and material support. West passes the visible proxy and exact typed-enlargement-ownership gate; east fails both (`0.001370683 m` residual p99 and non-exact enlarged ownership despite only `0.000337259 m` maximum residual difference), so the overall artifact is rejected. It receives exactly one correlated R0 development-site credit and zero independent-validation, OOD/holdout, production, preview, recipe-freeze, or publication credit; east is not Development C.
- [x] Close the correlated-A upstream/domain authority with exact routing-dependency evidence. The `+128/+256 m` transaction `802a3f47311b...` fails honestly with `65` canonical-only and `43` control-only dependency cells, D∞ branch target mismatches `30/11`, weight mismatches `22/22`, and `6` spill target/depth mismatches. Promoted `+256 m` authority passes independent `+384 m` control under transaction `e316add66854...` (manifest SHA-256 `c2ea6bf89f85...`): all `169,591` crop-contributing dependency cells are identical, every raw branch target/weight and finite spill target/capacity/order/depth is exact, all physical condition fields and derived `material_rule` are exact, and neither dependency graph touches its storage edge. Extraction-local `soil_feature_index` labels are nonphysical and are not an identity criterion. This authorizes solver use for the two correlated Development A crops only; it grants no independent-site, OOD, production, preview, recipe-freeze, or publication credit.
- [x] Bind the correlated evaluation through a narrow fail-closed loader to exact `e316add66854...` `+256 m` canonical and `+384 m` control domains. The retained path validates manifest/hash identity, solver authorization, closure pass, exact condition/D∞/spill/dependency claims, no storage-edge touch, and the exact canonical/control NPZ hashes before constructing either solver domain; ordinary A/C condition-bundle loading remains unchanged.
- [x] Run and reject all-positive reverse-D∞ branch competition artifact `58e82247ff50...`. Any-positive reverse-support overlap percolates across sheet flow and collapses `1,036` qualified heads to `89`, leaving west as deposition-dominated sparse islands. Both recognizable-form proxies fail even though mass, masks, rotation, partition, shared edge, and canonical/control ownership all pass exactly.
- [x] Run and reject the final deterministic dominant-flux arborescence candidate `bd945ad440f8...`. Disjoint dominant-flux capture basins and local physical/convergence maxima reduce `1,036` heads to `370` and visibly remove the west comb while retaining several rills, but both west and east recognizable-form proxies still fail. Preserve the artifact and park deterministic branch-initiation competition rather than tuning adjacency, scores, thresholds, or metric spacing.
- [x] Park the deterministic branch-initiation competition family. Reopen only for a structurally different initiation model or materially new measured initiation/branching evidence that preregisters the missing causal organization and can pass both correlated recognizable-form gates while retaining exact enlargement ownership, conservation, masks, rotation, partition, and shared edge. Score variants, neighborhood variants, fixed radii, threshold weakening, and any-positive D∞ overlap do not satisfy this condition.
- [x] Close the post-deterministic three-way hard-gate public-data hunt as a production-target no-find. No track found a corpus that jointly provides target pitch/error, machine-resolvable direct/interpolated support and surface semantics, transferable conditions, and usable rights. Preserve each exact resume condition; do not reinterpret the no-find as permission to weaken the gates or rehabilitate a failed corpus.
- [x] Complete the next-specialist decision by selecting a materially different, research-only combined rill-diffusion direction. Hinsberger may supply paired weak-height supervision only from original post-event rill DEMs; Alessio may supply morphology/topology/condition supervision and a frozen cross-source audit only. Neither source is qualified production truth, and the deterministic branch-initiation family remains parked.
- [x] Complete bounded retention for both rill corpora. Hinsberger selective-retention artifact `dec740173419...` preserves three DEM and three orthomosaic members from the `20,383,764,301`-byte DOI archive, but does not claim whole-archive MD5 equivalence. Alessio retains the exact `9,040,075,751`-byte MPK (SHA-256 `61d11524df35...`), four workbooks, and a `644`-file/`12,156,440,444`-logical-byte morphology extraction.
- [x] Run the frozen qualifiers and reject both as model authority. Final portable/revalidated Hinsberger artifact `3af7581156aa...` fails square-GSD, native-Z-unit, DEM/orthomosaic CRS/registration, direct-versus-interpolated support, event independence, total-error, and exclusion-mask gates. Alessio artifact `a3a4835b6843...` preserves six-site morphology/topology descriptors but fails absolute-height, CRS/XY-unit, vector/mask registration, total-error, workbook-binding, and cross-source requirements. Both are `research_diagnostic_only` / `not_authorized`.
- [x] Park agriculture and the combined rill-diffusion route as diagnostic-only. Neither may supply training authority, production truth, an immutable browser showcase, or recipe-freeze credit. Reopen only when the recorded semantic, registration, uncertainty, support, mask, and independent-group blockers are closed with machine-verifiable evidence; do not weaken the gates or relabel a single-source survivor as the combined route.
- [x] Select open, intact, non-forested `peat.raised_bog` as the next visually structural non-agricultural R0 route and add `peat-raised-bog-r0-research-bundle/1`. The machine preregistration preserves Moore's failed target result and authorizes no generation, training, transfer, production, `latest`, runtime, or wire-format claim until its remaining hashes are frozen.
- [x] Materialize metadata-only raised-bog site selection `46daa389e388...` (file SHA-256 `ff7cca92e676...`) without opening DTM, RGB, CIR, or detailed geometry pixels. Development mire `etak-component-0004069028` provides 209 eligible core-plus-halo footprints and the frozen core `[536384,6452416,536512,6452544]`; distinct sealed mire `etak-component-0004069025` retains exact unresolved soil semantics and may pass only by zero C1.
- [x] Run and reject the Moore whole-form descriptor/capacity checkpoint as corrected portable strict-replay artifact `cc811bc0425a...` (manifest SHA-256 `ecf647306241...`). Only one boundary-valid hollow across one geographic group survives, versus the frozen minimum of 200 complete typed forms across six groups; 68 components are boundary-truncated. Park this exact whole-form marked sampler before generation. Do not lower the gate, credit censored components as complete, pad the plots, or substitute repeated primitives/FBM.
- [x] Select and preregister the materially different censored-evidence-compatible method in `bundle-preregistration-v2.json` (SHA-256 `431b269d49f7...`): group-balanced, translation-edge-corrected HuHoLa class-pair evidence drives one hydrology-conditioned two-field plurigaussian whole-mire R0 hypothesis. It preserves the failed Moore B2 gate, has no independent fine-noise remainder, generates one canvas before crop-last chunking, and retains no production/`latest`/runtime/wire authority.
- [x] Materialize and reject v2 edge-corrected evidence as artifact `038e3695aee6...` (manifest SHA-256 `aa37bf8fd796...`). Exact HuHoLa support is 621 lawn cells, one hollow, and zero hummocks; required 0.5 m/1.0 m axial typed support, class-conditional margins, and all eight leave-one-group-out gates fail. Park v2 without parameter tuning, lawn-only synthesis, or fallback.
- [x] Preserve the reusable selected-mire condition snapshot `8d450c7fe056...` (file SHA-256 `1356bfa83916...`) as a fail-closed blocked artifact. Accepted corrected format-1 C0 covers only LOD0 `cx=148..155,cz=90..97`, not required `(0,81,89)`; the development halo therefore has zero C1 authority, and the sealed mire remains pixel-unopened. Strict replay re-audits the base release and binds `base.toml`, ETAK, every derived mask, coverage result, NPZ, and QA bytes.
- [x] Freeze one materially new whole-domain headcut/gully-event and depositional-toe R0 hypothesis as config `r0-headcut-event-v1.json` (SHA-256 `3efc46f38828...`) for the authorized Development-A closure. It does not vary the exhausted branch scores/adjacency/suppression/spacing family and excludes non-heightfield processes.
- [x] Run and reject the exact frozen headcut hypothesis. Original build `72f953d9588e...` produces 18 events but leaves east empty and changes the enlarged event catalog. Corrected final-audit build `0ecf3595eac2...` (manifest SHA-256 `ae4462d66fab...`) additionally fails closed because west's comb/repeated-fan exclusion was unevaluated and emitted crops contain `2.305059771 m3` incision versus `3.482331927 m3` deposition. Plan-volume equality and hydrology-off switch plumbing receive no positive credit. Do not pack or tune this candidate.
- [x] Park all current erodible-slope challengers: the deterministic branch-initiation family, unauthorized rill-diffusion evidence, and rejected headcut-event hypothesis. OOD2 ETAK `9688702` remains identity-frozen, unmaterialized, and unopened; do not spend another recipe-freeze cycle without a materially new authorized challenger satisfying the recorded resume condition.
- [x] Close existing-data visual-capacity triage. Biała Góra cannot support a learned/dictionary cliff owner at 6.25 cm; FORWARD plus the residual LUKE bank would be an unsupported macro/fine splice. The four pre-bank absolute LUKE surfaces remain a distinct exception: each is `256 x 256` at 0.0625 m with measured/confidence masks and retains 1-16 m layout.
- [x] Freeze and independently audit the Guérin-style sparse-representation R0 capacity screen before opening any surface arrays. Config `8ce53408eb2d...`, semantic anchor `979fbbd24839...`, trust root `361ec1209ab8...`, and spec `c08c65428be0...` bind the pinned paper/repository mechanism, exact construction/evaluation split, support-safe metrics, immutable QA/receipt chain, and exact implementation closure. No synthesis had run and k19 remained unopened at this checkpoint.
- [x] Run the exact construction stage once at checkpoint `273b50f` and park it on its frozen prerequisite failure. The first fold reached numeric QA but had no all-supported 64-cell spectral square, so the command failed after `0.68 s` before writing an artifact, PNG, trace, or receipt. No rerun or tuning is allowed; k19 remained unopened at this checkpoint. The screen therefore establishes no algorithm-capacity, Maa-amet, condition, transfer, production, or browser claim.
- [x] Implement the materially new organization-first erodible-slope challenger as an isolated `erodible_slope/morphodynamics/` package. It uses exact accepted structural C0/masks, one fixed nested canvas, authorized 1 m parent priority-flood organization, depression reservoirs, triangular-facet D∞ routing, extensive solid inventories, dimensionally correct branch-energy transport, finite sediment residence, analytical maturity feedback, conservative colluvial relaxation, typed hydrologic sources, and immutable artifact plumbing. Tiny synthetic physical/phase/resource invariants pass; no Development-A terrain run, cook, packing, or browser work has occurred.
- [x] Freeze and execute the exact Development-A morphodynamics recipe once. Final recipe `95b330dc9570...`, semantic identity `4ba5b980e3a6...`, and float artifact `c7deecd29239...` pass canonical/control identity, water/solid conservation, protected masks, and seams, but fail the frozen visible-morphology gate: C1 is nearly indistinguishable from C0 except sparse hairline incision and repeated short near-parallel combs along bank edges. Park this exact method without OOD, cook, browser, or tuning; retain its implementation and three labeled QA PNGs as negative evidence.
- [x] Execute the materially different multiscale-amplification specialist under the new outcome budget. Two complete Development-A attempts finished in about 15 minutes. Final artifact `0a150a358b3e...` preserves masks and conservative ledgers but still leaves C1 visually near C0, concentrates sparse inventory into a `4.9725 m` outlier, and exposes storage/support-aligned shoreline structures. Park it after attempt two; retain the cohesive package and labeled float/PNG artifacts, but do not cook, browse, or tune it.
- [x] Execute the next visually structural specialist from the already-audited exemplar family without reopening agriculture or extending the dataset hunt. The `forest.mesic_mineral` whole-form transport reached immutable float artifact `e67feb86a7b7...` on its second and final attempt: a `512 x 512` 6.25 cm canvas assembled from 25 support-qualified measured 10 m patches, balanced K11/K32/K36 `8/8/9`, with labeled surface, scale-band, and provenance PNGs. It passes the narrow R0 visual-capacity gate without the first attempt's exposed placement grid, but remains a foreign-analogue hypothesis with no Estonia condition, transfer, production, cook, browser, or `latest` authority. A generic exploratory NPZ summary opened k19 after the frozen sparse-screen checkpoint; k19 did not enter this artifact but is no longer an untouched holdout and must receive no future blind-test credit.
- [x] Run two bounded Estonia anchoring attempts for the positive forest capacity mechanism under the relaxed `generally should transfer` research rule. Both preserve hard exclusions exactly and retain one-metre C0 means, but fixed-grid overlap-add remains visibly unacceptable: attempt `8128ba6c...` has `13-14%` patch-boundary gradient excess, while final attempt `c9e9f2ed...` worsens to `74-77%`, exposes a square lattice in the ground-scale close-up, and reaches `0.513 m` residual. Park anchoring without cook/browser; this is an obvious repetition defect, not a minor evidence gap.
- [x] Replace fixed-grid anchoring with world-locked saturated blue-noise sites and a compact Wendland-C2 partition over the same measured 12 m LUKE forms. Final Estonia artifact `55ff85645ff...` passes the bounded research gate: no rectangular lattice or ownership ridges are visible, hard exclusions are exact, one-metre mean error is `1.56e-15 m`, residual RMS is `0.0393 m`, and P01/P99 are `-0.0894/+0.1053 m`. Treat this as a best-available `generally should transfer` forest research candidate, not national truth.
- [x] Complete the condition-owned, multi-site `forest.mesic_mineral` generalization checkpoint without cook or runtime work. Selection `f493174e558b...` freezes three non-overlapping 512 m parents 8.7-12.1 km apart inside canonical-base coverage, with distinct soil/geology signatures and `54.1-83.2%` coarse eligibility. Reject attempt `089e4eace548...`: overlap-applied patch offsets accumulated to `+7.11..+7.80 m`, projection concentrated that DC drift at selector boundaries, and output extrema reached `2.51-2.64 m`. Final artifact `5c7ab3ac6bb...` uses offsets only for DC-invariant candidate comparison and applies exactly zero patch offset. Its three full `8192 x 8192` 6.25 cm absolute C1 masters retain `54.0-83.1%` exact eligibility, zero hard-exclusion residual, maximum one-metre mean error `5.62e-16 m`, boundary ratios below `0.894`, `0.355-0.430 m` output extrema inside the measured `1.503 m` envelope, and cross-site nearest-copy RMSE `0.0475 m`. This is a float research candidate with no production, cook, pack, browser, or national-owner authority.
- [x] Publish the strongest full-parent forest generalization candidate through the existing ordinary height hierarchy without changing formats, runtime synthesis, or shaders. Recipe `6d28b79a393f...` splits the frozen northcentral `8192 x 8192` C1 master into all 16 LOD -2 children, derives byte-exact parent `-1/606/361`, and masked-splices corrected LOD0-L4 ancestry. Immutable non-`latest` manifest SHA-256 `48bacfc559d1c5f626168d753d4ef3ec46394847ef2d6ad5f3894936641b540d` independently verifies zero decoded seams, `0.00099945 m` maximum C1 round-trip error, `0.00156737 m` maximum LOD0 closure error, zero hard-exclusion residual, and inherited vegetation/material/water inventories. Exact lower-altitude Estonia URL `http://localhost:5182/?scene=world&src=estonia&dataurl=http%3A%2F%2Flocalhost%3A8792&alt=12&x=310528&z=185088&yaw=0&pitch=-0.22&shadowclipres=896` passed real Chromium/WebGPU boot through frame `158` with no page, HTTP, asset-stream, TSL, WebGPU, pipeline, or bind-group error. This is one 512 m, `83.05%`-eligible mesic-mineral-forest research preview, not production or national coverage.
- [x] Cook and independently verify the accepted irregular forest surface through existing LOD -2/-1 and masked LOD0-L4 closure. Recipe `38f861e9aec5...` publishes immutable non-`latest` manifest `716289d00291...`; four changed LOD -2 children, parent `-1/607/372`, excluded C0, decoded seams, byte-exact parent derivation, and inherited vegetation/material/water layers pass. The exact Estonia URL on fresh ports `5181/8789` passed real Chromium/WebGPU boot through frame `146` with no page, TSL, WebGPU, pipeline, or asset errors.
- [x] Complete the pinned Schott paper/repository algorithm preflight. Do not port revision `64fe87...` as a candidate: the release uses bilinear rather than claimed bicubic refinement, never reads bound hardness, closes erosion/deposition boundaries, wraps thermal boundaries with evolving simplex noise, omits paper retarget/breach components, uses empirical dimensionally unbound constants, and is unvalidated below roughly metre cells. Retain only its multiscale operator sequence and ablation structure. The candidate must be rederived around continuous/rotation-qualified routing, canonical world samples, whole-domain outlets/upstream flux, physical-unit erodibility/runoff/seep/material fields, conservative thermal/sediment flux, explicit masks, and one absolute master cropped only after the domain solve.
- [x] Acquire and freeze the authoritative EGT 1:200k `Q_Litoloogia_200` and `Q_Genees_200` code domains. Commit `7ca8f4b` and condition bundle `77b81b2dc051...` bind the official decoded values and unknown policy; the 1:50k similarly named domains remain forbidden substitutes.
- [ ] For the first target claim that passes semantics/support/error, complete the three-site/two-campaign split, physical-analogue dossier, Estonia condition/OOD/sentinel bounds, full condition fabric, and user-approved target-specific preregistration. Keep Järvselja sealed until that freeze.
- [ ] Run the per-regime specialist bakeoff and select an owner only if it beats the corrected structural baseline on the frozen blind gates. A valid no-winner leaves that regime morphology-free.
- [x] Complete user visual investigation of the full-parent `forest.mesic_mineral` preview and lock it as this regime's current research owner with status `research_preview_investigated`. The accepted software checkpoint is commit `62c2a6e515196685adb49357478006336e39d57e`, exact recipe `6d28b79a393fe377d51629906f7608f6ac66630b34074f07589413fff590af1c`, immutable manifest SHA-256 `48bacfc559d1c5f626168d753d4ef3ec46394847ef2d6ad5f3894936641b540d`, and exact frame-158-booted URL `http://localhost:5182/?scene=world&src=estonia&dataurl=http%3A%2F%2Flocalhost%3A8792&alt=12&x=310528&z=185088&yaw=0&pitch=-0.22&shadowclipres=896`. The user found the result natural, physically right, and free of visible weirdness. This accepts one bounded mesic-mineral-forest research preview only: it is not production, `latest`, national coverage, multi-parent continuity, another forest regime, or any non-forest regime. Preserve the agriculture hierarchy preflight as a reusable worked example: decoded-child updates must be masked-spliced through every affected independently cooked LOD1-L4 ancestor; stale ancestors and whole-parent reduction are forbidden.
- [x] Generalize ordinary fine-height publication to an arbitrary finite parent set and publish the three frozen disjoint `forest.mesic_mineral` sites in one immutable non-`latest` format-2 manifest. Recipe `dffa2ca2e3d0...`, manifest SHA-256 `8459fa8f5a7f281923398170daf3719fbed0d17bac2408ebe67af7bbd43a9a87`, and micro-verification SHA-256 `acf7739053d3...` bind 48 LOD -2 children, three byte-exact decoded-child LOD -1 parents, 27 deduplicated transient supports, and one three-core corrected-base transaction. Maximum C1 round-trip error is `0.00099945 m`, maximum masked LOD0 closure error is `0.00156737 m`, decoded seams and hard-exclusion residual are zero, and inherited biome/boulder/canopy/debris/soil/tree/understory/water indexes are byte-exact. Exact southwest, northcentral, and east WebGPU boots on `:5183/:8793` passed at frames `105/98/90` with visible inherited layers and grounded grass. The first end-to-end attempt failed only because the verifier incorrectly required planned byte-identical no-op replacements to differ; the corrected final attempt proves every actual change is transaction-owned and no unrelated ancestor changed.
- [x] Complete blind user visual investigation of that exact three-parent preview and mark recipe `dffa2ca2e3d0...` / manifest `8459fa8f5a7f...` `research_preview_investigated`. Southwest felt natural at overview and close range, with varied bumpiness but less visible material variation and a teal cast; northcentral felt especially natural through green ground and small dirt/stone lines; east remained natural and resembled southwest, perhaps slightly smoother. With grass disabled, the user could see credible `6-20 cm` microdetail and restrained `30-70 cm` macrodetail; a separate grass-enabled pass remained grounded and visually sound. Lock this forest morphology across the three investigated sites. Treat color, shading, and material differences as observed inherited site context rather than a proven synthesis defect; this does not authorize production, `latest`, national coverage, adjacent-parent continuity, another forest regime, or any non-forest regime.
- [x] Extend the locked `forest.mesic_mineral` morphology across one contiguous north/south two-parent float master without retuning its accepted generator. Commit `a14b008`, artifact `4e5762bb9a41...`, and four numbered QA PNGs bind parents `(617,377)` and `(617,378)` from one `512 x 1024 m` world-locked solve before cropping. Exactly 351 placed forms cross the shared boundary; seam residual step mean/p95 `0.01939/0.04999 m` is below nearby non-seam `0.02399/0.06548 m`, one-metre closure is `9.39e-16 m`, hard residual is zero, and K11/K32/K36 ownership is balanced `11251/11252/11251`. Common-light and 32 m ground-scale QA show no seam, phase reset, or morphology discontinuity.
- [x] Pack and independently verify that exact adjacent-parent master through the unchanged ordinary fine-height hierarchy at commit `1c9e647`. Build `d1cdf0768c0f...`, manifest SHA-256 `b97c88a68bb7a7e6a9c64ba5ec37778971dd76aeb6c65c5d813fd16f5ffd423c`, and all seven verifier gates bind all required LOD -2 children, both LOD -1 parents, support, and affected ancestors. Maximum C1 round-trip error is `0.00099945 m`, LOD0 closure is `0.00156403 m`, shared child seam is zero, shared parent decoded seam is `3.8147e-6 m` below the explicit `1e-5 m` limit, and inherited layers are unchanged. Exact all-layer/`grass=0` WebGPU boots on `:5187/:8797` pass at frames `89/125` with no captured page, console, request, TSL, or WebGPU errors; materials, trees, understory, grass, and grounding remain present. Canonical `latest` remains unchanged.
- [x] Complete user visual investigation of the exact adjacent-parent preview at `http://localhost:5187/?scene=world&src=estonia&dataurl=http%3A%2F%2Flocalhost%3A8797&alt=12&x=316160&z=193536&yaw=0&pitch=-0.22&shadowclipres=896`. The user flew freely around the review area without being given a compass direction and observed no seam, straight boundary, or detail reset. The forest floor looked natural and possibly more diverse than the prior view; the visible ditch is inherited C0 macrostructure rather than a cliff test. Mark adjacent-parent `forest.mesic_mineral` continuity `research_preview_investigated` and lock the accepted generator/ownership behavior. This does not authorize production, `latest`, national coverage, another forest regime, or any non-forest regime.
- [x] Extend the locked `forest.mesic_mineral` generator without retuning across one contiguous four-parent `1024 x 1024 m` master containing a full two-axis junction and a broad eligibility/hard-mask transition. Float artifact `41ff0ad0ecda...` retains exact hard exclusions and one-metre C0 means, places 707/708 cross-axis sites plus six cross-junction sites, and keeps seam p95 residual steps (`0.05155/0.04733 m`) below nearby non-seam values (`0.06461/0.06345 m`). Pack recipe `dc4c28617df3...`, manifest SHA-256 `7563ea2267a2...`, and verification `2f8380d95373...` bind all four LOD -1 parents, 64 published LOD -2 children, 17 transient supports, zero decoded child seams, four parent-boundary comparisons below `1e-5 m`, `0.00099945 m` maximum C1 round-trip error, `0.00156403 m` LOD0 closure, and byte-exact inherited non-height layers. No runtime, shader, wire-format, or `latest` change occurred.
- [x] Complete user visual investigation of four exact independent starts inside that four-parent preview on `:5189/:8799`. Every position looked natural, detailed, and at least as varied as the earlier accepted forest views; no seams, square patterns, glitches, ownership boundaries, or unnatural repetition were visible around any spawn. Lock the four-parent transition result as `research_preview_investigated`. The apparently small finest-detail area is the unchanged runtime visibility policy: LOD -2 is fully weighted only within `32 m` and fades out by `40 m`, although the complete four-parent fine payload is packed and available. This is not a missing-data or failed-cook issue and does not grant production, `latest`, national coverage, another forest regime, or non-forest authority.
- [x] Complete and review bounded mapped-rock/till float candidate `8dbbe1393d37...` on the `512 x 512 m` Estonia window `[683520,6435840,684032,6436352]`. The `2048 x 2048` surface at `0.25 m` spacing places three different FORWARD body/socket families at ETAK type-10 single boulders `1145271`, `1145269`, and `1145643`; a fourth family supplies one weak, non-repeated source-derived till-fabric form. All three anchors have complete official `Loimis1` and `Huumus` parses and decoded EGT `50/50` glacial-till/moraine support. Type-20 boulder piles `1147668`, `1147669`, and `1145228` remain hard exclusions. Parent visual review accepts the three paired closeups as distinct embedded refinements rather than circular/square islands; every hard-mask and outer-border residual is exactly zero. This is a bounded float research-capacity candidate only, not user acceptance, generic till, boulder-pile synthesis, Estonia transfer/truth, 6.25 cm fine rock detail, packing/browser authority, production, cliff, or sandstone capability.
- [x] Complete and park the stronger full-parent rough-till/mapped-boulder exemplar-field challenger from commit `1c87c5c`, artifact `27af29e19748...`. Its single `2048 x 2048` master preserves hard exclusions, the outer border, the accepted mapped boulders, and all four measured FORWARD source families. Common-light review nevertheless exposes granular source-patch texture, rounded or rectangular activity islands, abrupt family blocks, and no coherent till/clast/socket/depositional organization. Preserve the deterministic source selection, world-domain assembly, body/socket replacement, configuration, floats, and four numbered QA PNGs as reusable negative evidence; do not pack, tune patch placement, or claim generic till capability.
- [ ] Expand by independently qualified Estonia regimes before any wide or national claim.
- [ ] Require every rock/cliff specialist to reconstruct evidence-supported macro geometry before adding fine morphology. Under-resolved outcrop faces, shoulders, edges, ledges, fractures, scarps, bank breaks, talus transitions, and shoreline profiles must be corrected or reconstructed cook-side where the active representation can express them; merely making coarse DTM rock shapes or inherited ramps bumpier is a failed specialist, not a partial success. Before activation, every materially different region or condition that may receive cliff edges or rocky geometry needs an inspectable representative preview whose visual gate judges macro correction and fine detail together, including the transition into adjacent ordinary terrain.
- [x] Complete the bounded Biała Góra source-domain macro-plus-fine representation screen on the support-qualified 128 m core `[202636.5,684821.5,202764.5,684949.5]` in EPSG:2180. Attempt 1 `a07b7a8c4742...` is rejected because it reconstructed unknown class-2 gaps and exposed a broadband raw fine residual. Final attempt `9d27b52da864...` publishes only direct qualified cells (`97.6807%` of the core), replaces the raw residual with a band-limited `0.5-1.25 m` component, and preserves the connected crest/face/toe break, asymmetric bench, slump-headwall sockets/aprons, branching chute/gully, and ordinary-ground transition without line-normal ribs, finite-line caps, slabs, or a merely roughened ramp. The fine-component diagnostic remains locally granular, so the positive claim is connected macro-form representation plus bounded source-band retention, not realistic fine synthesis. The float artifact and four numbered QA PNGs are source-domain calibration evidence only: vendor class 2 is not authoritative truth, no predictive Estonia transfer or 6.25 cm support is established, and no packing/browser/runtime work is authorized.

### Accepted infrastructure

- [x] Re-audit the dirty, unpushed implementation and isolate new work in a clean worktree based on `258ddef`.
- [x] Restore the canonical shared grid: height LOD0 remains 1 m with 2048 m chunks; negative height-only rungs provide 0.25 m and 0.0625 m samples.
- [x] Implement signed negative-LOD transport, LAC2 decoding, near-only demand, and packed-height sampling.
- [x] Fix fine-height use by terrain and grass without disabling inherited trees, materials, or vegetation.
- [x] Compile procedural `terrainDispAt` geometry out of format-2 Estonia terrain and grass roots; packed terrain is now the only geometric surface while generated/legacy worlds retain existing behavior.
- [x] Center the hero coverage on `58.107506 N, 27.050242 E` in LOD -1 parent `(607,372)`.
- [x] Select and pin the LUKE Lapinjarvi TLS pilot corpus: k11/k32/k36 calibration and k19 holdout, CC BY 4.0.
- [x] Reject the first TLS ground extraction because vegetation/object returns formed elevated terrain islands.
- [x] Replace extraction with a center-seeded, connected, multi-view ground-manifold method and strict fail-closed gates.
- [x] Rerun corrected extraction on all four real TLS plots and inspect numerical plus rendered surface QA.
- [x] Approve extraction-valid surfaces and build the measured residual bank: 149 calibration patches (k11=42, k32=49, k36=58); k19 excluded as holdout.
- [x] Fix the half-texel synthesis coordinate contract.
- [x] Wire the measured synthesis cook CLI and recipe-kind checks.
- [x] Add bounded production-preview verification and prohibit pilot publication to `latest`.
- [x] Apply direct fine-grid ETAK masks for open water, buildings, and paved roads; deliberately retain unpaved trails.
- [x] Cook the historical rejected-LUKE 512 m hero closure and materialize immutable diagnostic preview `5f845ae2...`, manifest `b1c7c44bad667fd7`, with its independent packing/mask gates passing.
- [x] Restrict the Lapinjarvi matrix to exact ETAK forest plus fail-closed unmodified mineral/mesic soil context. Published hero allowance is `53.3685%`; four chunks correctly receive zero residual and unsupported samples remain base-only.
- [x] Replace the fixed 3 m normalized-crossfade quilt with crop-stable equal-power overlap, normalized edge-shape cost, and balanced `top_k=12`; focused calibration diagnostics restore bank RMS/tails and effective use to `140.3/149` patches.
- [x] Replace the inherited per-1 m bubble projection with one AOI-wide smooth mean-null solve plus fail-closed boundary taper. The real 5x5 closure has exact 1 m means (`8.83e-17 m`), zero hard-mask residual, zero shared-overlap error, no `0.747 m` support spike, and stays inside the measured bank amplitude/slope envelope.
- [x] Replace hard finest-level switching with a format-2-only packed-level geomorph shared by terrain, grass, material normals/slopes, and CPU ground probes. The former `14.96 cm` (-2/-1) and `34.89 cm` (-1/0) ownership steps now fade through bounded quintic transition rings.
- [x] Replace the eager raster prepass allocation with a compact projected-record pool and a shared 96 Ki cluster domain. Project/context allocation drops from about `716 MiB` to `133.5 MiB`, with fail-closed observable overflow.
- [x] Add a strict real-Chromium WebGPU boot gate and fix every exposed validation failure: redundant texture uniforms, non-uniform workgroup barriers, writable storage aliases, and mixed access modes in a single synchronization scope.
- [x] Inspect the supplied Suur Taevaskoda reference and categorize the representable shore/bank/top-surface failures separately from the vertical/undercut wall.
- [x] Correct the premise audit: the main vertical/undercut sandstone wall is not representable by a heightfield and is excluded from this pass. Any non-heightfield structural solution requires its own future research and user-reviewed spec.
- [x] Confirm and locate anomalous pinned-base height returns inside the mapped Ahja river near the reference anchor.
- [x] Implement the general evidence-bounded river-surface/base-DTM conditioning pass and materialize the corrected Stage 1 base release through transaction `009fd...`; independent verification, the exact-URL WebGPU boot, and user visual acceptance all pass.
- [x] Supersede the earlier spec's false raw-DTM consistency objective and record diagnostic-only LUKE scope, source-repair authority, heightfield limits, executable conditioning, and fixed Taevaskoda review requirements.
- [x] Materialize and independently verify historical rejected-LUKE recipe `090747d74911...`, manifest `eaa0444646b47de1`. Its coverage, header, apron, hierarchy, determinism, and decoded hard-mask gates passed; `latest` remained untouched.
- [x] Complete the historical rejected-LUKE user visual checkpoint on `:5180` / immutable data endpoint `:8791`; this does not constitute a live URL for the current `009fd...` Stage 1 transaction.
- [x] Pass three clean exact-URL WebGPU boots for that historical rejected-LUKE checkpoint after removing diagnostics, including cloud bake, settled frames, and zero page/console/TSL/WebGPU errors; active `009fd...` now has its own independent frame-106 pass.
- [x] Integrate clean work into the main `estonia-asset-gen` worktree as local commit `0ef75cf` without pushing; superseded root edits remain preserved in a safety stash and were not reapplied.

### Rejected synthesis and current research phase

- [x] Reject the LUKE quilt / mean-null residual synthesizer as a production or beauty method after user-visible review. It adds sparse noise-like bumpiness, does not repair 1 m grid shorelines or source defects, lacks Estonia substrate/process grounding, and is not capable evidence for national 6.25 cm detail.
- [x] Retain the proven negative-LOD cook, packing, serving, streaming, decoding, rendering, masks, terrain/grass grounding, and strict real-WebGPU boot infrastructure; disclose the remaining generic tree/boulder/understory/debris packed-surface sampling gap without redesigning the format or adding runtime synthesis.
- [x] Record the repository-wide research and feature-decomposition standard in root `AGENTS.md`.
- [x] Inventory every paper, repository, dataset, and consequential claim cited by `r1.md`, `r2.md`, and the superseded spec; correct citation conflations and report overclaims. The closed v2 ledger has 129 unified records including 21 repositories and exact one-to-one classification of all 141 checksum-manifest artifacts; the uncited Burren dimensions remain explicitly excluded.
- [x] Download every legally accessible full paper and identified repository artifact; record canonical URLs, licenses, hashes, access failures, demonstrated scales, officialness, and review depth without equating a valid tarball with code review. All 141 recorded local artifacts pass the checksum ledger.
- [x] Produce independent learned-model and deterministic/process/exemplar proposals grounded in full primary sources and the explicitly bounded repository evidence; consequential reuse still requires officialness/license/code-review closure.
- [x] Commission separate adversarial critics for each proposal; proposal authors did not critique their own work.
- [x] Expand the synthesis problem into Estonia's actual surface-forming regimes, observation-error correction, covariates, representation limits, and sub-decimeter training/validation requirements (`review/REGIME-PHENOMENON-MATRIX.md`, `review/TARGET-SCALE-DATA-AND-REGIME-AUDIT.md`).
- [x] Audit current and official Estonia-wide conditioning inputs, including source fidelity loss, EGT coverage, and Taevaskoda's mapped ETAK/soil constraints (`review/ESTONIA-CONDITIONING-DATA-AUDIT.md`).
- [x] Audit the actual cook, hierarchy, correction-authority, coverage, verifier, storage, and unchanged-runtime contract (`review/COOK-CONTRACT-AND-HIERARCHY-AUDIT.md`).
- [x] Decide orthophoto acquisition: fetch/version dated RGB/CIR as masked research and likely production conditioning, never as metric height truth; complete the national manifest-size scan and the content-addressed RGB/CIR/ETAK Stage-1 QA artifact before bulk acquisition.
- [x] Write the comparative architecture dossier and source/claim ledger (`review/COMPARATIVE-ARCHITECTURE-DOSSIER.md`, `library/SOURCES.md`, `library/sources.json`).
- [x] Choose one decisive evidence-gated typed-hybrid architecture: deterministic evidence fusion and regime graph, per-regime target-supported specialist bakeoff with valid no-winner, deterministic absolute-surface composition, and unchanged packed-height delivery.
- [x] Add executable regime/evidence, hierarchy/quantization, visual-review, and storage/cost contracts; independently critique the first spec rewrite and correct its water, authority, hierarchy, visual, and staging contradictions.
- [x] Normalize the source ledger to schema v2 and retain the first Stage 1 method contract as an explicitly superseded negative-design record after final audit disproved its Moore ownership test (`library/sources.schema.json`, `review/STAGE1-EXECUTABLE-METHOD-CONTRACT.md`).
- [x] Materialize the strict 26-row regime schema, initial registry, and status manifest; final audit leaves all production morphology unsupported and Moore target evidence insufficient rather than inventing an owner.
- [x] Rewrite `docs/specs/terrain/MICROTOPOGRAPHY.md` from the audited evidence, with one decisive replacement architecture and executable structural, evidence, release, runtime, and visual gates.
- [x] Run independent science/beauty, repository/cook, and standalone-evidence audits of the integrated spec and close every critical/high finding without hiding introduced runtime/release/data dependencies.
- [x] Record the user's zero-external-budget decision: paid Estonia acquisition and a separately funded non-heightfield cliff project are rejected unless explicitly reopened.
- [x] Audit public equivalent target data in Latvia/Lithuania, Finland/Sweden, and Estonia/Taevaskoda through separate primary-source evidence hunts; classify actual qualification rather than nominal resolution.
- [x] Fold qualifying public-data findings and the closed product decisions into the durable spec, move it to `docs/specs/terrain/MICROTOPOGRAPHY.md`, and update every authority reference. Candidate binaries enter the machine ledger only when selected for conversion.
- [x] Check in `PUBLIC-TARGET-QUALIFICATION-PROTOCOL.md` and machine ledger records for the exact selective Hovi Järvselja/Hyytiälä files and exact Taevaskoda ALS epochs before retaining or converting them.
- [x] Freeze the bounded zero-cost conversion inputs: 43 Hovi files totaling 7,241,800,875 bytes, eight official Taevaskoda ALS epochs, and one 351,166,481-byte Biała Góra calibration epoch; do not authorize bulk Biała acquisition.
- [x] Add an independently audited, content-addressed Taevaskoda ALS retention path that binds the frozen selection, source/license snapshots, response provenance, actual bytes, and partial/complete state.
- [x] Retain and inventory the eight Taevaskoda ALS epochs (190,445,551 bytes total); keep them `calibration_only` for typed structural repair and never treat them as 6.25 cm morphology truth.
- [x] Qualify the retained-tile multi-epoch Ahja water/shore observations: authorize the 2019 class-9 profile, reject DTM-only/class-2 authority and every unsupported epoch, and preserve bank/cliff abstention (`review/TAEVASKODA-ALS-WATER-QUALIFICATION.md`).
- [x] Retain and inventory the smallest verified adjacent 2019 ALS selection needed by the exact fine publication/support domain: north tile `445679` and east tile `444680`, 87,992,146 raw bytes total, independently hash-bound from the primary eight-epoch selection.
- [x] Close Ahja topology qualification: `2356024` is one unbranched confluence-to-confluence reach with no internal mapped hydraulic jump; bind the local cross-section-capped water polygon and reject full-polygon endpoint clamping (`review/AHJA-HYDRAULIC-TOPOLOGY-QUALIFICATION.md`).
- [x] Materialize and independently replay the provenance-bound three-tile 2019 campaign evidence (`c84d0b59e0de...`) without inventing continuity: the downstream segment from full-line station 71 is qualified with a 15 m longest unsupported span, mapped wet outside qualified support remains explicit morphology-forbidden abstention, and no bridge observation mask is fabricated.
- [x] Resolve the evidence-driven structural closure before cooking: promote adjacent LOD0 core `(152,93)`, retain 45 LOD -1 and 777 LOD -2 transient reducers, and bind the separate 897-tile paired authority/baseline input halo while publishing only the selected 16+1 negative review chunks.
- [x] Replace raw-GeoTIFF bilinear baseline ownership with exact-mean prolongation of the pinned browser-decoded LOD0 base; retain the raw DTM only as hash-bound repair evidence.
- [x] Implement bounded disk-backed structural materialization for 897 paired inputs, 777 LOD -2 reducers, 45 LOD -1 reducers, and two corrected LOD0 cores without changing runtime formats.
- [x] Replace whole-core LOD1-L4 rederivation with exact masked decoded-child window splicing into the independent inherited DTM rungs; preserve all unrelated decoded samples bit-identically, including apron-only neighbors, and fail closed if a required qoffset retune cannot prove this.
- [x] Unify coarse and fine flowing-water closure on one halo-aware iterative rule: conservative bed is the full-weight target, terrain remains below the qualified water surface, and fine authority cannot silently reauthorize a coarse abstention.
- [x] Persist content-addressed fine authority/abstention masks and corrected-water transaction inventories for independent dry/abstained restoration verification.
- [x] Complete Stage 1 Taevaskoda typed structural repair, corrected parent hierarchy, staging, and corrected format-1 release through immutable transaction `009fd...` step 07; retain `7a2d...` only as superseded build history.
- [x] Complete `009fd...` independent preview verification after the verifier-only quantization/snapshot fixes, mark the transaction `releaseReady:true`, pass the exact-URL real-WebGPU boot at frame 106, and complete user visual acceptance of the immutable corrected-source result; no morphology-specialist claim is part of this release.
- [x] Run the bounded structural-only natural-escarpment/bank-break macro screen on non-sentinel Development A ETAK `1826743` and Development C ETAK `9688719`, using accepted C0, A domain closure `e316add...`, ETAK, soil/geology, RGB/CIR, and hard masks. Both complete attempts are immutably rejected: attempt 1 `386f5a42...` leaked finite-line endpoint ownership into broad wedges; corrected attempt 2 `4f66a8b9...` preserves every hard mask but still produces visible finite-line caps/slabs and exceeds its 2 m structural-correction envelope. The two-attempt limit is exhausted, so Taevaskoda parent `(-1,607,372)` remains unopened and no packing/browser work occurred.
- [x] Run the separately authorized, structurally different global-2D variational escarpment screen on the same non-sentinel A/C evidence, bound only as generic sand/till bank-escarpment development and explicitly not sandstone authority. Attempt 1 `ca17522e...` proves exact hard/endpoint masks and a bounded displacement solve but is rejected for nearest-frame ribs and fixed-boundary jumps. Attempt 2 `e95c1b9c...` replaces that frame with continuous polyline projection and adds a zero collar; it remains rejected because both shaded previews retain visible cross-line ribbing and the active/fixed boundary jump is still `0.459301 m` on A and `0.092414 m` on C. The new two-attempt limit is exhausted. Preserve the global sparse-surface implementation and QA, keep cliff-capable regimes abstained, and do not open Taevaskoda or pack a candidate.
- [x] Complete the Development-A-only Estonia ALS-anchored adjacent-escarpment macro screen as float artifact `a5d101c1cca4...`. The genuinely 2D second-order TGV solve uses 22,194 qualified raw 2019 class-2 points over both adjacent chunks plus their halo, Huber data attachment, C0/collar priors, and vegetation/shadow-masked RGB/CIR anisotropic regularization. Withheld 8 m block-median error improves on both mapped sides (`0.099944 -> 0.094533 m` low, `0.031089 -> 0.025524 m` high); all 235/235 mapped face cells, all hard masks, and the outer collar remain exact C0. This is development-only measured macro capacity: no Biala morphology enters the solve, no Taevaskoda sentinel is opened, and no packing/browser/production/fine-rock credit is granted.
- [x] Materialize that exact Development-A ALS/TGV float artifact as an explicitly authorized immutable non-`latest` research preview without changing runtime code, shaders, formats, or server protocol. Recipe `1f7e3220914c...`, manifest SHA-256 `bbba982d9f9ade4f2fdfd582c7758f7c3997fb4851b0614c44ebbba62c5bd8cf`, and independent verification `37df3527a0fa...` carry the complete nonzero artifact halo to its zero collar through four LOD -1 parents, 64 published LOD -2 children, and 17 transient supports. The verifier proves 144 fine and all adjacent-parent decoded seams exactly zero, byte-exact child-derived parents, `0.00100327 m` maximum structural round-trip error, `0.00100136 m` maximum conservative hard-C0 error, `0.00500274 m` masked LOD0 closure, exact artifact hard/mapped-face/collar residuals, no stale hierarchy changes, and byte-exact inherited non-height indexes. Exact normal and `grass=0` WebGPU boots on `:5185/:8795` pass at frames 100/86 with terrain continuity, materials, trees/understory, and grounded grass present. The LOD -2 rung reconstructs the measured `0.25 m` structural authority and makes no morphology claim below `0.25 m`.
- [x] Complete user visual investigation of the Development-A packed ALS/TGV preview and reject it as a full escarpment macro specialist. The wavy face structure is a positive but modest ALS-anchored feature. Shoulder/crest macro is sparse, terminates abruptly, and leaves long visually boring gaps; the face macro remains too soft. The apparent holes coincide with the river but are not deep bed pits: ALS/TGV residual is exactly zero on sampled water nodes, and correct 2 m water sampling gives `0.605724 m` maximum depth, `0.259611 m` p99 depth, zero samples deeper than `1 m`, and only `0.101318/0.289154 m` at the cited locations. The prior multi-metre reading was a water/height-grid indexing error; no repair is warranted because the large C0 corrections remove the false raw-DTM/TIN bridge. The separate right-angle white/green strip was confirmed as an absolute-C0 packing-ownership splice and is closed by commit `a478443`, replacement recipe `34cb26cc3be64a7a68963f1f83658520b5e7b65a2a94e0cade2d4012904ea6f9`, and manifest SHA-256 `8ed4f6f7ca2a21aad914ea7952455ad6c2c028358396faa571361452f76bba93`: verification SHA-256 `da7045c14c32b44fcdb39db1f0b75e60dfc744ca2c212f21a4daf13fc35e4b6b` passes 16,384 former-edge comparisons with `0.00197703 m` maximum error, zero fine and parent seams, `0.00500274 m` LOD0 closure, and byte-exact inherited indexes; exact normal/`grass=0` WebGPU boots on `:5186/:8796` pass at frames 104/101. Review the fixed geometry at `http://localhost:5186/?scene=world&src=estonia&dataurl=http%3A%2F%2Flocalhost%3A8796&alt=8&x=311911.76&z=191069.20&yaw=0&pitch=-0.28&grass=0&shadowclipres=896`. This fixes packing ownership only and does not upgrade the rejected ALS morphology. Retain the float reconstruction and verified ordinary packing as reusable measured structural-correction and delivery proof, but grant no morphology-owner, production, national, cliff, or fine-rock credit.
- [x] Complete and park the bounded robust-ALS structural-reference replacement after its single correction cycle. Attempt 1 `684449eb5f54...` preserves all exact hard laws and improves the low-side holdout (`0.068180 -> 0.061757 m`), but slightly worsens the high side (`0.033200 -> 0.034114 m`), its 0.05-confidence-contour gate is not an ownership boundary, and the float residual remains locally granular. Corrected attempt 2 `ed91840fac78...` uses only the 6 m macro fit as height authority and measures the actual zero-support handoff; it passes exact hard/face/collar and transition gates, but fails the low-side holdout (`0.068180 -> 0.073761 m`) and its common-light/residual QA still shows measurement islands rather than connected macro landforms. Preserve the LAS 1.4 scan-angle correction, robust plane/quadric confidence fields, connected break/ridge/valley extraction, float artifacts, and four labeled PNGs; do not pack either result or mislabel structural reconstruction as Stage 2E process morphology.
- [x] Complete and park the whole-domain Biała exemplar-field challenger from commit `2b26a03` after one attempt and one consolidated correction. Attempt 1 `43d2706c6ea5...` passes mechanical gates but routes away the source's paired signed organization. Final artifact `0855afa3a7cb...` preserves the paired 2D field, routes only added target-conditioned incision/seep relief into conservative toes, solves one crop-last `769 x 1281` master at `0.25 m`, and keeps every hard, water, mapped-face, protected, and collar residual exactly zero. Erosion/deposition are `856.448120/856.448181 m3` with `3.56327e-8` relative conservation error; relief is `-0.517907/+0.390438 m` with `0.201024 m` absolute p95. Reject it visually: common-light macro remains too soft, the right bank forms long paired generator-shaped bands, lower slopes form broad lobes, and seep relief is negligible. Preserve its hash-bound registration, signed composition/transport code, floats, typed ownership, and four numbered QA PNGs as negative evidence; it is not worth packing and establishes no Estonia transfer or production authority.
- [x] Complete and park the Biała paired sparse/dictionary typed irregular-transport challenger at commit `576bfa6` and immutable float artifact `361e3b0c59f5...`. The coupled dictionary honestly uses only paired `1 m` low / `0.5 m` high Biała atoms; its `0.25 m` output lattice is interpolation plus existing C0 authority, not finer truth. All hard, water, mapped-face, protected, and collar residuals are exactly zero and maximum one-metre mean error is `2.81761e-9 m`, but common-light QA exposes a dense checker/waffle phase, alternating signed bands, and patterned bumpiness across slopes and toes. Only `3/1175` placements are incision-owned, so the result does not recover connected gullies despite 214 distinct source centers. Preserve the typed coupled-OMP dictionaries, irregular Wendland-C2 partition, C0/ALS conditioning, attribution arrays, floats, and four numbered QA PNGs as negative evidence; do not pack or continue dictionary, overlap, atom, or closure-projection tuning.
- [x] Complete and park the bounded per-regime Biała single-field multiscale learned challenger at commit `b58e486` and immutable artifact `a0054c3616c9...`. Its three-scale terrain-native generator trained for 1,500 MPS updates, preserves every hard, mapped-face, and collar sample exactly, avoids direct source copying, and produces visible relief (`-0.46/+0.46 m`, `0.102497 m` absolute p95). Reject it before packing: common-light QA reads as widespread mottled learned texture plus alternating bank-edge stamps, not coherent converging incision or connected crest/shoulder/face organization. The apparent toe holes beside the river remain visually inconclusive and are not used as rejection evidence. Preserve the checkpoint, floats, isolated implementation, and four numbered QA PNGs; do not tune this single-site patch-statistics family further because one Biała field does not encode the target's long-range source-to-toe process organization.
- [x] Complete and park the two-stage domain-scale metric pixel-diffusion challenger at commit `e7d2e97` and immutable artifact `597b54f64206...`. Stage 1 owns 2 m crest/shoulder/face/toe organization over 128 m contexts; Stage 2 is correctly subordinate and owns only the 1-2 m innovation band. Every hard, mapped-face, and collar sample is exact, but the target coarse field saturates into broad alternating lobes and striated relief (`0.574635 m` absolute p95, `+/-0.72 m` extrema), while Stage 2 adds mottled squiggles. The geographically held-out same-site Biała block nearly loses the real incision (`0.403929/0.081892` generated delta/gradient RMS versus `0.762499/0.158739` weak-surface RMS). Only four nonoverlapping direct-support 128 m training windows remain after holdout and buffer, so this is a corpus-capacity failure rather than a tunable diffusion defect. Preserve the isolated code, cache, floats, and four QA PNGs; do not spend the correction cycle, pack, or continue guidance/amplitude/loss tuning.
- [x] Resolve and close the Stage 2E long-context evidence probe without bulk acquisition. Metadata could not distinguish process diversity among the nine unfetched Biała epochs, so retain only the cheapest exact probe, `2022-11-10.zip` (`509,963,696` bytes, SHA-256 `609bd72589c7...`), under the existing 2 GiB cache and 64 GiB reserve. February and November use compatible LAS/PF3/EPSG:2180 and identical 0.5 m direct-support semantics; rigid registration is `dx=0`, `dy=-0.05 m`, `dz=-0.0038 m`. Final comparison `c2f38d123d44...` increases qualified nonoverlapping 128 m windows from 4 to 6 but geographic union remains 6, with both additions merely completing endpoints of the same diagonal cliff corridor/anatomy. Park multi-epoch training and the remaining eight archives: no materially new connected morphology was gained.
- [ ] Keep the cliff-capable Stage 2E regime abstained until a materially new public long-context source or explicitly authorized process-diverse corpus appears. Do not download more Biała epochs blindly, retrain diffusion on duplicate anatomy, or use the river-adjacent toe as synthesis evidence. Move active work to the strongest already-retained non-cliff regime rather than spending another algorithm cycle on a closed data-capacity blocker.
- [x] Materialize the accepted bounded rough-till/mapped-boulder float artifact `8dbbe1393d37...` through the ordinary immutable fine-height hierarchy without changing runtime code, shaders, or formats. Final build `f2aaedefa692...` and manifest SHA-256 `ccbc3eefa360b6edec80cfa3c972a35e270e2cd8cd6df8ff14a1b23b0ddf5e0a` publish all 16 LOD -2 children plus byte-exact decoded-child parent `(-1,615,389)`, use nine transient support chunks, and masked-splice the affected LOD0-L4 ancestry only. Independent verification proves 40 decoded seams at exactly zero, `0.00100327 m` maximum structural round-trip error, `0.00500465 m` maximum masked LOD0 closure error, zero artifact forbidden residual, no stale ancestor changes, and byte-exact inherited biome/boulder/canopy/debris/soil/tree/understory/water indexes. Exact real-WebGPU boots on `:5184/:8794` passed at frames `118` and `108`; the all-layer view retains materials, vegetation, and grounded grass, while the `grass=0` view exposes continuous non-lattice geometry. This is an immutable non-`latest` research preview over one 512 m parent. Its LOD -2 rung is structural Keys reconstruction only and makes no morphology claim below the source artifact's `0.25 m` pitch.
- [x] Complete user visual investigation of exact build `f2aaedefa692...` / manifest `ccbc3eefa360...` as a neutral capacity pass, not strong acceptance. The user saw the inherited exposed boulder plus a shallow, varying mound/socket and judged it “fineish,” with little else in this bounded view to evaluate. No obvious artifact was reported, but the result is too narrow and subtle to establish a rough-till owner or receive production, national, cliff, or fine-rock credit.
- [ ] Implement a morphology specialist only after its public target subset passes conversion, support/error/semantics QA, analogue/OOD qualification, and a target-specific preregistration.

## Deliberately Deferred Until The Research Review

- Any further beauty claim or national rollout of the rejected LUKE synthesizer.
- Peat, agriculture, carbonate/alvar, sandstone, shore/coast, fluvial, wetland, forest-event, engineered-ground, and other regime synthesis without matching evidence and exemplars.
- National orthophoto bulk acquisition until the full official manifest is scanned for size and the research ablation proves useful geometric conditioning rather than albedo-to-height leakage.
- Broad test expansion while extractor, generator, and release contracts remain in active change.
- National flowing-water activation until Jägala waterfall and other mapped hydraulic discontinuities pass blind protected-feature regression without smoothing, ramping, or moving the jump.
- Any non-heightfield cliff representation; it is a separate future research/spec task, not a hidden extension of this heightfield pass.
