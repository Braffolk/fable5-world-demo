# Microtopography Orchestrator Digest

Derived, non-normative digest for orchestration. Authority remains
docs/specs/terrain/MICROTOPOGRAPHY.md and the task ledgers. Regenerate if the spec changes.

Covers spec sections 9.10-23 and the still-open/binding items of
docs/tasks/2026-07-13/KNOWN-ISSUES.md. Jump to the cited section for normative text.

---

## Part A — Spec 9.10-23 (binding rules, gates, budgets, DoD)

### 9.10 Raised-bog R0 research bundle
- Authorizes ONE deterministic R0 challenger for open, intact, non-forested `peat.raised_bog`
  hummock-lawn-hollow relief. Preregistration: `.../peat-raised-bog/bundle-preregistration-v1.json`.
- Grants NO training, pixelwise height supervision, target truth, Estonia transfer, production
  owner, or `latest`. Moore stays plot-local descriptive R0 evidence only (zero strict-B1, failed
  B2 — not relaxed). HuHoLa may label/diagnose, not generate.
- Before generation, machine contract must hash-bind: 1 metadata-selected dev mire, 1 different
  SEALED abstention mire, corrected base, peat/humus + mire conditions, hard/unknown masks, C0/C1
  recipes, code/env, seeds, budgets, thresholds, cameras, visual rule. Selection reads structured
  metadata ONLY; excludes Taevaskoda; audit pixels sealed until recipe+dev result frozen.
- C1 = one whole-domain conditional marked-form/process hypothesis; must pass capacity gate; may
  NOT substitute a jittered primitive, copied relief, periodic lattice, FBM, or generic roughness.
  Generate over connected mire + full halo, crop chunks LAST. Per-chunk seeds/seam crossfades forbidden.
- Only preregistered canonical seed advances; sensitivity seeds may only reject. Survivor reaches at
  most an immutable non-`latest` preview (§14.6).

### 9.10.1 Censored-evidence v2 replacement
- V1 (SHA `21d97272...`) is immutable REJECTED history; its whole-form C1 route (artifact `cc811bc0...`,
  1 hollow / 1 group vs frozen 200-form/6-group gate, 68 boundary-rejected) is permanently terminated.
- V2 contract: `.../bundle-preregistration-v2.json`. Changes the evidence UNIT (group-balanced HuHoLa
  class fractions, transiograms, HHDH margins) to inform ONE hydrology-conditioned two-field
  plurigaussian R0 hypothesis. Generate over connected mire + ≥64 m halo, sample only continuous
  typed form-margin surface at 0.0625 m. FBM/noise/copied/repeated/per-chunk/condition-edge relief
  forbidden. Cannot authorize production or `latest`.

### 9.11 LUKE absolute-surface sparse capacity exception
- Narrow exception (else LUKE excluded by §9.7). Preregistration
  `.../forest-sparse/ideal-self-degradation-r0-v1.json` (full-config SHA
  `8ce53408eb2df83dfbd9b529c173e12d2eedbdfb1be494dbd3625096556a43de`; semantic SHA `979fbbd24839...`).
- Four pre-bank absolute 256×256 @0.0625 m surfaces enter ONE ideal-self-degradation screen of the
  pinned Guérin mechanism. k11/k32/k36 supply atoms in 3 leave-one-plot-out folds. **k19 stays
  UNOPENED** until every construction numeric gate passes AND a human-authored outside-generator
  receipt hash-binds every review file + passes every visual gate. k19 may not supply an atom,
  threshold, parameter, selection, retry, or fallback.
- Operator: confidence-weighted 1 m obs, 8×8 low ↔ 128×128 fine patches, 2 m stride, sparsity-one OMP.
  Fine relief needs 32-cell-eroded 4σ Gaussian; spectral QA needs a fully supported square (no
  zero-filled void edge). All copy/quilt/phase/noise/void gates frozen before k19 opens.
- Trust root: `.../forest_sparse/frozen_anchor.py` (SHA `361ec1209ab8...`), hardcodes semantic anchor
  + SHA map for the OTHER outcome files; cannot self-hash. Package must contain EXACTLY 7 anchored
  `.py` files, no symlink/bytecode/shadow. `PYTHONPYCACHEPREFIX=/tmp/assetgen-forest-sparse-pycache`.
- Grants NO degradation/error model, transfer, condition responsiveness, packing, cook, browser,
  `latest`, runtime, or shader authority. Any failure parks Guérin/LUKE WITHOUT tuning.

### 9.11.1 Post-screen bounded forest research-preview escalation
- **2026-07-16 user authorization**: measured K11/K32/K36 whole-form carrier MAY be composed in world
  coords, packed through ordinary negative-height hierarchy, and previewed in an immutable non-`latest`
  Estonia browser preview under the relaxed "generally should transfer" research rule.
- Remains `research_only`: no target truth, campaign independence, transfer, owner, `pilot`,
  `national`, or `latest`. Locked generator scale-testable WITHOUT retuning. Pit/mound, managed/clear-cut
  forest, other soils, and every non-forest regime require SEPARATE owners. Production promotion still
  requires §§9.2-9.5, 12.6.
- LOD0 FLOAT scale test = immutable artifact `6d3bae2fa0ed...`: one 32768×32768 abs 0.0625 m master over
  LOD0 `(0,154,94)`; 263,799 placements balanced 87,933/87,933/87,933; zero hard-exclusion residual;
  `6.28e-16 m` max 1 m-mean error; `0.03410 m` residual RMS; p01/p99 `-0.09122/+0.11101`; max abs
  residual `0.45333 m`. FLOAT/mechanical only — PNG review, packing, cost, WebGPU boots, user review pending.

### 10. Typed Structural Reconstruction (source repair precedes synthesis)
- **10.1 Solver**: absolute EH2000 on LOD-1 centers per repair component + 8 m collar. float64 sparse,
  0.25 m 4-neighbor gradients, cut-aware 5-pt Laplacian; delete edges across hard barriers. Constraint
  priority list is fixed; same/higher-level conflicts REJECT the component (record both source IDs) —
  never average. Huber cutoff 1.345. Never estimate noise from `y−median_3x3(y)`. Every obs joins hashed
  `repair-calibration-v1`; uncalibrated records cannot authorize automatic removal. ell ∈ {0.5,1,2,4} m
  chosen per class/stratum needing ≥30 shape-matched pseudo-voids + ≥30 protected counterexamples.
  IRLS ≤20 steps, MINRES rtol 1e-10 ≤5000 iters. **Reject-component thresholds**: hard error >1e-4 m;
  normalized KKT residual >1e-8; unexplained trusted error >max(0.10 m, 4σ); overshoot >max(0.25 m,
  4σ_ref); protected-jump change >max(0.02 m, 5%) outside water toe; new unsupported drainage dam >0.05 m.
- **10.2 Water/shore/bank/bed**: distinct surfaces (waterY, packed height incl. bed, watercover, shoreline,
  bank, escarpment) from ONE evidence snapshot. Raster-compat is a RELEASE GATE: p95 contour ≤0.5 m,
  max ≤1 m, no dry gap/wet overlap >0.5 m on 0.25 m cross-sections; channels ≥4 m min width must stay
  connected. Standing water needs ≥64 qualified samples + robust spread ≤0.15 m else abstain. Unbranched
  reach: 1 m stations, ≥4 samples/station, 11 m Hampel, isotonic if oriented, σ4 m Gaussian; missing span
  >20 m / branched / >0.25 m bank-inconsistent abstains. Ungauged bathymetry = `unknown_bathymetry_render_envelope`
  `depth=0.10*smootherstep(clamp(d/1m))`, confidence 0, no morphology. Bed composed into height BEFORE LOD-2
  composition; any later bed/shore/occupancy change invalidates affected height artifacts. Ahja `E679788.35,
  N6444811.45` is a review case, not a coordinate patch.
- **10.3** Typed classes (leakage, spikes/pits, strips, ditches, corridors, quarry, protected boulders/cliffs).
  Every automatic class needs protected counterexamples; removing a real cliff/bank/boulder/depression = hard fail.
- **10.4 Corrected authority extent**: transient sets are EXACT — 45 LOD-1 reducers (`cx 604..612, cz 372..376`),
  777 LOD-2 reducers (`cx 2416..2452, cz 1488..1508`), 897 paired 0.25 m authority/baseline (Keys halo),
  16 LOD-2 review cores + LOD-1 `(607,372)`, corrected LOD0 cores `(0,151,93)`+`(0,152,93)`. qoffset failure
  fail-closed; expectation frozen independently.
- **10.5** Structural-only finest: Keys cubic `a=−0.5` bubble to 0.0625 m, exact structural parent mean,
  a reconstruction operator NOT terrain detail. Reapply shoreline/bed at fine centers.

### 11. Regime & Phenomenon Graph
- Catalog PIN `estonia-microtopography-phenomena/2026-07-13.1` SHA
  `6ca03d5bb552f6ad83e818da1d16d3fddf7d4006241990a90f6ffc21ab13ee4d`. Any change to a phenomenon /
  eligibility / condition / scale / limit → new catalog version, invalidates affected rows.
- **11.1** Every row validates `microtopography-regime-release/1.0.0`, `additionalProperties=false`, 13
  required groups (identity, physical_definition, eligibility, transition, structural/representation,
  conditions, target_evidence, geographic_splits w/ `taevaskoda_excluded=true`, candidate_bakeoff w/
  `allow_no_winner=true`, band_evidence, morphology_and_visual_result, packing_cook_serving_cost,
  failure_abstention_release). Eligibility AST `laas-condition-ast/1`; missing/failed-confidence/drift →
  `unknown` → abstain (never false-nearest). Cross-field gates: `pilot` (2 dev + 1 blind site, qualification,
  leakage, band evidence, owner, cost, extent, user approval); `national` (estonia_national ceiling, owner,
  audit, approval); `research` (foreign data allowed, name is NOT a production owner, bands stay research/unsupported);
  `target_evidenced_effectively_smooth` (95% UCB on recoverable signal below preregistered threshold);
  `released` (supports_claim=true, frozen owner, visual pass, cost, extent, approval). 3 checked-in artifacts
  (registry schema, initial rows, initial status) with pinned SHAs.
- **11.2** Initial registry = 26 rows: 25 `unsupported`, `peat.raised_bog`=`research`, ZERO Estonia targets/
  owners/approvals. `research` never authorizes production packing.
- **11.3** Coverage audit binds `microtopography-claim-domain/1.0.0`; terminals per band: `R`(released),
  `S`(smooth-proven), `F`(water/hard-structure), `N`(non-heightfield), `U`(unknown/research-only), `A`(conflict).
  Full morphology claim FAILS on any `U`/`A`/count gap/overlap/hash change. Research previews are NEVER inputs
  to production coverage (research cells stay `U`). Separate `microtopography-research-preview-domain/1.0.0`.

### 12. Specialist Framework
- **12.1** Required challengers per production regime on same train/dev/blind sites: corrected-only,
  exemplar, conditional simulation, process/event, regressor, cGAN, pixel-diffusion, hybrids. LUKE quilt
  is NOT the bar. Research screen may run a smaller preregistered subset (not the production bakeoff).
- **12.2-12.5** Learned: metric height in pixel space, NO natural-image RGB VAE, no SD init, mask by measured
  support, freeze one production realization. Diffusion = default high-ceiling CHALLENGER, not default owner.
  Process/event: repeated jittered primitive is NOT a distribution. Statistical: spectra alone = noise.
- **12.6 State machines**: research chain `research_declared → trained → development_survivor →
  research_recipe_frozen → research_audit_pass → research_preview_candidate → investigated | rejected`;
  any pre-preview → `research_rejected` (terminal on any audit fail / post-audit tuning / prohibited data /
  copied relief / OOD overconfidence / hierarchy-runtime fail). Research reports may NOT claim real-vs-real
  non-inferiority, RMSE, truth, production, transfer. Production owner: preregistration + user approval BEFORE
  any training; select lexicographically (discard gate-failers → non-inferiority to real-vs-real reference →
  blind render win → `tie_no_owner` → freeze). States: `target_evidence_insufficient`, `no_candidate_survives`,
  `tie_no_owner`, or owner.
- **12.7 Moore screen**: archive is 0.01 m NN-interpolated + 0.03 m mean-filtered, NOT 1 cm truth; result stays
  `target_evidence_insufficient` — cannot rank/authorize a production winner. M0 (ideal synthetic 1 m→0.0625 m)
  vs M1 (0.25 m oracle ablation, never production-shaped).
- **12.7.1 Narrow M0 activation** (`moore-m0-research-design.2026-07-15.json`): reserves D/R/E + corrected-only;
  authorizes NO training until a separate hash-bound candidate-selection supplement exists. Output is one joint
  absolute `Hhat` @0.0625 m; registered outputs = B1 + `derived_B2_hypothesis` (mandatory name). **B2 four-phase
  gate (before training AND on every held-out output)**: ≥6 of 8 groups usable; `max_g R_g ≤ 0.25`; CV_g ≤0.15
  at median, ≤0.25 in every used group; median group B2 RMS ≥0.01 m; radial PSD 7-8 cyc/m ≤0.15 median /≤0.25
  used vs 4-8 cyc/m; morphology selected-relief/eligible-relief ≥0.70/group; packing: decoded max height error
  ≤0.005 m, decoded B2 energy 0.90-1.10, persistent-form relief ≥0.90 retained, zero parent/seam failures. B2
  fail TERMINATES Moore as ≤0.1 m preview route. 68 grids, 8 leave-one-geographic-group-out folds, 3,091,387
  finite 0.01 m cells / 309.1387 m². `demsmooth.m` SHA `729f5488...`. 7 candidate recipes E/S/P/R/G/D/H (prose only,
  not machine configs). R/G/D/H: float32 train, AdamW 2e-4, batch 32, clip 1, 50,000 updates/fold/seed, seeds
  {17,31,47} aggregated, ≤3.5M params, no pretrained. No real-vs-real envelope / owner / tie declared.

### 13. Canonical Composition & Determinism
- **13.1** Composer ownership order (corrected structure → long-range form → within-form realization →
  cross-scale effects → transitions → protected/water/hard/representation). Two specialists on same
  phenomenon+scale = config error; never average. Research preview: exactly one frozen candidate per
  sample/band; masks reapplied after candidate; production ignores every research claim.
- **13.2** Analysis bands via separable F4: `c=[1,3,6,10,12,12,10,6,3,1]/64`; R4 = Keys cubic `a=−0.5`,
  phases {−3/8,−1/8,+1/8,+3/8}. Identity `H2=R4(R4(A0)+B1)+B2`, B1=0.25-1 m band, B2=0.125-0.25 m band.
  Packed hierarchy uses fixed 4×4 decoded-child area reducer, NOT F4.
- **13.3** Global stochastic identity: keyed BLAKE2b-256, key = 32 recipe-seed bytes, personalization
  ASCII `laas-micro-prf1`, 112-byte record (fields fixed). Same world point → same field in every window;
  storage-chunk coords are NOT keys; periodic boundaries forbidden; worker count/order/AOI must not change
  accepted bytes.
- **13.4** ML env: pinned optional `micro-ml`, deterministic kernels, fail-closed, never silent procedural
  fallback. Bit identity required on locked stack; different stack = different recipe, requalify.

### 14. Cook, Hierarchy, Release
- **14.1** Three extents: model support (unpublished), hierarchy support (unpublished), publication coverage
  (published). Nine transient child chunks = hierarchy support only, NOT an ML/process halo.
- **14.2** Canonical 16-step order (bind snapshot → solve water/bed → corrected LOD-1 + bed → one abs LOD-2
  master over publication+support → crop → `encode_quant16_checked` → decode-accept → reduce to LOD-1 →
  combine → corrected LOD0 → splice ONLY affected LOD1-4 windows → recook waterY/watercover/veg → publish
  format-1 base → pin format-2 overlay). Never replace an inherited LOD1-4 core with a child pyramid; never
  project accepted fine geometry back to a raw error.
- **14.3** Next proof publishes one complete structural-only LOD-1 parent + 16 LOD-2 children. Multi-parent/
  national needs generalized `HeroCoverage`/expectation/metadata/auditor/verifier first (LAC payload, v2 index,
  server, runtime plane unchanged).
- **14.4 Quantization** qscales: LOD-2 0.002 m, LOD-1 0.005 m, LOD0-2 0.01 m. Reject nonfinite/overflow, never
  clip; error bound `max|d−h| ≤ s/2 + 2·|spacing(float32(max|d|))|`. Corrected-base closure is mask-based:
  `M_{l+1}=any(M_l)` over 4×4 window; samples outside mask stay bit-identical. Policy
  `preserve-inherited-or-retune-full-payload-outside-bitexact/1` — preserve inherited codes outside mask; on
  overflow only, retune full payload but every unmasked decoded value stays bit-identical, else fail closed.
  National qoffset needs deterministic grouping/seam policy (one 0.002 m u16 span ≈ 131.07 m).
- **14.5** Recipe identity hashes all sources/registry/bundles/algorithm/regime rows/specialist code/env/seed/
  extents/reducer/qscales/codec/corrected-base. Artifact reusable only when complete recipe+deps match.
- **14.6 Transactional publication**: old `release.py` cannot authorize (pins old base SHA, old 17 keys) —
  must be replaced, not bypassed. Three recipe kinds: `corrected-structural-base-v1` (stages format-1 base,
  no `latest`), `evidence-microtopography-v1` (binds corrected-base SHA, production), `research-microtopography-preview-v1`
  (binds accepted corrected base + literal metadata `authority=research_only`, `target_truth=false`,
  `production_owner=null`, `estonia_transfer=none`, `release_row_unchanged=true`, `latest_eligible=false`).
  **Publication order**: freeze expectation docs+hashes → stage+verify corrected base → materialize format-1
  (no `latest`) → bind SHA + stage evidence → verify merged format-2 + claim coverage → runtime+visual gates
  → **after explicit user acceptance, atomically update `latest.json` EXACTLY ONCE**. Research-preview order (7
  steps) never touches canonical `latest.json`; release tooling must hard-reject a research manifest on the
  `latest` path. No screenshot/boot/preference can promote a research manifest.

### 15. Orthophoto Acquisition
- Fetch required for research + likely production. RGB 20-40 cm / 10-16 cm dense; capture date mandatory.
- **15.1** Stage-1 snapshot bound to DTM sheet `54472` SHA `a70ddfeeec08...`, ETAK GPKG SHA `e19084c4...`,
  workbook SHA `fbef1433...`. RGB `54472_OF_RGB..._2025_07_18.zip`, CIR `..._2024_05_22.zip`. Validate CRS 3301,
  transform, bounds, bands; fail closed on HTML/changed row/missing CRS/wrong schema. ETAK shoreline = Stage-1
  boundary authority; RGB/CIR is QA + scored challenger only when human-QA mask clear, registration p95 ≤0.25 m,
  date compatible, every proposed displacement ≤1 m. Otherwise ETAK stays accepted.

### 16. Verification & Acceptance
- **16.1** Production data gates (target support/error §9, independent-site + physical-analogue §9.3,
  disjoint train/dev/blind, valid semantics/licenses, signal > error in every claimed band). **16.1.1**
  Research-only gates: bundle artifacts + no prohibited access, validated `ResearchSurfaceEvidence` (R0/R1/R2)
  or §9.9 R0 contract, no geometry outside research_trainable cells, frozen leakage groups, candidate state
  `research_preview_candidate` before packing, literal non-production metadata. Missing survey error is NOT a
  research failure — it is mandatory proof production gates stay failed.
- **16.2** Source-repair gates (delta by class, fit within registration uncertainty, water continuity/no bridge,
  smooth shoreline no 1 m staircase, preserved bank/escarpment, protected false-correction rate). Destroying a
  real protected feature = hard fail.
- **16.3** Morphology gates per regime+site (height/slope/normal/curvature/relief, spectrum/variogram, typed form
  geometry, distribution precision+coverage, memorization, periodicity, counterfactuals, transitions). Thresholds
  from held-out real-vs-real. Low-RMSE flat AND spectrally-correct noise both FAIL. **16.4** 1 m grid leakage:
  reject phase-locked amplitude, raster staircase, repeated bowls/ridges, pattern change at LOD/chunk edges.
  **16.5** Hierarchy gates (headers/CRC/hashes, quantization, decoded aprons, byte-exact decoded-child parent,
  bit-exact inherited outside masks, ancestor closure, partition/AOI/worker determinism, water/bed compatibility,
  inherited-layer presence).
- **16.6 Blind rendered beauty gate**: reference `reference/suur-taevaskoda1.jpg` 1920×1278 SHA `f031ef58...` (comp
  ref, NOT metric truth). Materialize hashed `visual-review-v1.json` before blind inference. Path `scene=world&src=estonia&
  seed=1&freeze=1&mschart=0&hud=0&dpr=1&shadowclipres=896`, FOV 55°, 1920×1080, T=12.5 + light 8.5/17.5. Origin
  `(311123.082,190723.435)`, cameras S0..S4, paths P1-lateral(361)/P2-dolly(481)/P3-orbit(361)/P4-light(301). **9
  independent reviewers**; morphology finalist needs **≥8/9 wins** vs corrected-only (and vs prior owner if any);
  `P[X≥8|n=9,p=.5]=0.01953125`; each hard-gate item ≥8/9. Objective immediate fails: missing layers, failed WebGPU,
  seam/normal/parent-closure fail, static water displacement, detail absent in cluster view, reproducible P2 pop/crack.
  User is separate final authority; metrics cannot waive a visible hard fail. **16.6.1** Research-preview gate: same rig,
  narrower question; Taevaskoda viewed ONLY after freeze, never to tune; may NOT use truth/target/qualified/owner/pilot/
  national/accepted-morphology; result = `research_preview_investigated`|`research_rejected`, changes no production row.
- **16.7 Real runtime gate**: freeze `PREVIEW_REL`, `EXPECTED_MANIFEST_SHA256`, URL. `boot-smoke.ts --manifest-sha
  --evidence` fetches/verifies `latest.json`==expected. Evidence schema `laas-webgpu-boot-evidence/1.0.0` (all diagnostics
  incl. empty arrays + `pass`); missing evidence = fail. Research preview: canonical repo `latest.json` bytes identical
  before+after boot, manifest `latest_eligible=false`, URL/evidence say `research_only`. No shader change except §4.5.
  Do NOT hand the user a URL until this boot passes. §4.5 manifest `ce4cf01c...` passed geology-on 88/70, off 90 — GPU perf pending.
- **16.8** Focused tests only for stable contracts; do not build a brittle suite around provisional internals;
  tests never replace real WebGPU boot + visual review.

### 17. Resource & Serving Budget
- Zero-runtime-synthesis accepts stored geometry but storage isn't free. 45,000 km²: LOD-2 = 11.52 trillion
  samples / 23.04 TB raw / ~2.75 M chunks; LOD-2+-1 raw = 24.48 TB. One raw u16 payload = 8,396,802 B + 56 B
  header. **National rectangle = 101,418.27072 km²** (NOT 45,000 land-only): raw total `55,225,799,191,680 B`
  ≈ 55.23 TB, ~18.25 TB compressed at the rejected-LUKE ratio; aligned 16.384 km pilot ≈ 48.3 GB compressed /
  146.2 GB raw. Old ~8.3 TB estimate is UNSAFE (mixed ideal land + rejected compression). Every candidate/release
  must report full train/inference/memory/bytes/entropy/object-count/egress/recook ledger; p50/p95/p99 nearest-rank
  (`x[ceil(p·n)]`); p99 `insufficient-n` below 1,000 obs; totals use exact sums never p50×count. Quality gates
  precede cost; cost only breaks ties among quality-equivalent survivors. National shape + retained-build policy
  need explicit user approval.

### 18. Staged Work
- **Stage 0** freeze evidence/interfaces. **Stage 1** (cheapest decisive proof): Track A (public-evidence closure,
  Moore audit → `target_evidence_insufficient` w/o training, qualification protocol), Track B (Taevaskoda typed
  repair — the decisive deliverable), Track C (optional Moore M0 screen; if source-B2 fails produce no ≤0.1 m
  preview). No PRODUCTION owner selection until qualified Estonia/§9.3 targets + user-approved preregistration.
- **Stage 2R** first weak-evidence FOREST research specialist (active): freeze bundle+preregistration, develop
  semantics on ForestSemantic train1-4 then run test1-2 ONCE, build HY_SPRUCE4 evidence, train subset, freeze
  recipe, then Evo + HY_PINE2 + open `JS_SPRUCE1` ONCE. **Stage 2E** erodible-slope R0 (Dev A/C then sealed OOD2
  once). **Stage 2P** first production regime (Hovi Järvselja/Hyytiälä conversion first). **Stage 3** parent-closed
  production pilot. **Stage 4** regime expansion. **Stage 5** wide/national (needs generalized tooling + user auth).

### 19. File-Level Map (map, not authorization)
- **19.1** Replace exemplar/residual facade with typed contracts; delete `micro_synth.py` (no fallback).
- **19.2-19.4** Absolute-master cook, decoded-child hierarchy, retire LOD0-mutating waterbed branch, set/verify
  `WATERBED_FLAG`, replace old recipe-kind/fixed-17 dispatch, boot-smoke/serve-data only for manifest selection.
- **19.5 Runtime (streamed-instance grounding)**: ONE minimal representation-sampling repair authorized; NO
  synthesis/shader/format/decoder/cache change. In `src/nanite/world/ChunkContent.ts` replace LOD0-only grounding
  for streamed trees/boulders with the generic packed finest/morph sampler + coverage fallback used by terrain/grass.
  In `src/gpu/passes/UnderstoryScatter.ts` remove hard-coded `{lod:0}` and use the same sampler. Grounding must
  refresh when packed morph center/ownership changes (caching one initial Y is invalid); a point on a band boundary
  gets the same height+weight as terrain. Preserve instance identity/X-Z/density/materials/culling/scatter. Prove
  terrain/grass/tree/boulder/understory/debris root agreement at fixed poses AND crossing both negative-rung rings —
  no float/sink/pop/island. If impossible via the generic representation, STOP for architecture review.

### 20. Rejected Alternatives
- Runtime procedural displacement, global 128 m LOD0 rewrite, raw-DTM DDNM projection, universal
  diffusion/regressor/GAN/dictionary/process library, geostatistical roughness, orthophoto displacement, storage-
  rung-mandated two ×4 nets, per-chunk generation/seeding, nearest-regime fallback, mm features because grid is
  fine, heightfield-only Taevaskoda wall, binary/shader redesign for the next proof.

### 21. User Decisions
- 2026-07-13: paid acquisition/proprietary/hardware/vendors/side-projects REJECTED unless user reopens budget;
  neighboring public data must be exhausted (§9.3 transfer); no non-heightfield cliff project. 2026-07-14: incomplete
  survey-truth may drive evidence-tiered research supervision; first research bundle frozen (§9.7); research output
  packs ONLY as immutable non-`latest` preview. 2026-07-15: bounded Stage 2E R0 authorized (input blockers unrelaxed).
  Remaining product decision = wide/national coverage + retained-build storage after measured pilot entropy.
- Checkpoints: (1) arch review done 2026-07-13; (2) Stage-1 corrected-source live URL; (3) Stage-2R research URL;
  (4) Stage-2E R0 URL; (5) first production-qualified regime; (6) explicit approval for any multi-parent/national cook.

### 22. Definition of Done
- Every claim-domain cell has exactly one valid terminal (R/S/F/N; no U/A for a full morphology claim); every
  claimed common regime target-supported; source errors corrected via published hierarchy; finest surface visibly
  adds realistic varied process-correct geometry; unsupported regions have NO filler; water/shore/bank/bed/escarpment
  correctly typed; no 1 m/chunk/apron/regime-boundary/repetition/LOD artifacts; terrain/water/materials/trees/grass
  agree on packed surface; clean boot on exact immutable Estonia URL; cost + national policy approved; user accepts.
  A cook that passes numbers but looks like noise/quilt/smoother triangulation is NOT done.
- **22.1** Stage 2R complete only with full bundle chain under one recipe hash; terminal `research_preview_investigated`
  or `research_rejected`, NEVER project DoD; leaves all production cells `U`, owners null, rows + `latest.json`
  unchanged. **22.2** Stage 2E identical terminal/effects, carries no weak-surface/positive-audit/transfer claim.

### 23. Normative Research Record
- **23.1** `library/sources.json` validates `microtopography-source-ledger/2.0.0` before any literature record can
  authorize a specialist owner. Ledger SHA `812988e5605d...`, schema SHA `8d6911d6...`, 141-artifact integrity
  manifest SHA `30bd0c0e...`. 129 records (21 pinned repos); only 10 allow consequential judgment. Unknown authors/
  licenses/officialness stay BLOCKING. Burren clint/grike excluded; two Argudo HTML masquerades quarantined.
- **23.2** Required reading list (r1.md, r2.md, review/*, contracts, library). `STAGE1-EXECUTABLE-METHOD-CONTRACT.md`
  is a SUPERSEDED negative-design record — do NOT implement. Key source boundaries: Guérin 2016 (paired sparse, not
  universal), Argudo 2017 (multilayer exemplar, NOT the orthophoto FCN — that is Argudo 2018), Schott 2024 (hydraulic
  specialist, not universal), Moore 2019 (measured peat, not national transfer).

---

## Part B — KNOWN-ISSUES: open / binding items

- **Current synthesizer REJECTED; only infrastructure accepted.** Residual bank/quilt/masks/hierarchy/preview proved
  the transport but geometry rejected (weak noise-like relief, bumpy water, staircase shorelines, no substrate model).
  No replacement morphology impl authorized before a target-specific preregistration passes the reviewed spec.
- **No production-ready target corpus.** Dense public raw obs exist (Hovi, Evo, FORWARD, Biała) but none is a qualified
  bare-earth target. 0.0625 m output needs support finer than ~0.125 m min wavelength + error/support/semantics/pairing/
  holdouts. Every unsupported regime must retain corrected structural terrain, no invented residual. Paid acquisition +
  side-projects rejected; closure path = selective conversion + Estonia OOD sentinels. Spec may not quietly restore
  paid acquisition.
- **Hovi HY_SPRUCE4 = scientific NO-GO** (`target_evidence_insufficient`). Full E57 decoded (all 4,248,797,321 records,
  103 GB materialized, manifest `ed02d209...`), central 6.25 cm shard dominated by competing/unresolved sheets (234/4096
  single-sheet). Reusable decoder/materialization/QA retained; parked from surface/target/transfer/synthesis. Sealed
  Järvselja `JS_SPRUCE1` UNTOUCHED. `e57==0.2.3` `read_points()` FORBIDDEN (would need ~158 GiB). Full-read contract:
  28-byte ABI, 4 m shards, hard 120 GiB output ceiling, ≥216 GiB free + 96 GiB reserve.
- **Evo plots 1086/1065** retained raw candidates w/ malformed LAS 1.4 headers (recovered via fixed-chunk sequential).
  1065 better support (99.75% vs 98.28% all-return) but both `surface_semantics_and_error_unresolved` — parked; treeid=0
  is NOT a ground class, z−h not independent truth.
- **FORWARD Marrviken DTM** (source SHA `592ce462...`) = one-site processed boulder/rough-forest evidence w/ visible
  kriging fans; not ground truth, not transfer-qualified. Helicopter dates predate machine campaign.
- **8 Taevaskoda ALS epochs** retained (190,445,551 B). 2023/2024 files have a PROJ-rejected compound WKT — must NOT be
  silently normalized (EPSG:3301 + EH2000).
- **Corrected fine terrain cannot stay pinned to raw LOD0** — CLOSED for Stage-1: transaction `009fd3d2be07...` published
  corrected format-1 base + format-2 overlay, steps 01-07, `verified-preview-release`, `releaseReady:true`. Prior `7a2d7653...`
  is superseded history. Ahja crosses `(0,151,93)` east edge → `(0,152,93)` corrected core. Working closure = 45/777/897/2.
- **Preview verification + user structural acceptance = PASS.** Accepted recipe `74fdccda...`, manifest `6c7c108f...`,
  data `:8788`. Fresh Chromium WebGPU through frame 106 at centered URL `x=311123.082&z=190723.435`. User confirmed river
  anomalies gone. Accepts Stage-1 STRUCTURAL repair only — no morphology/beauty, no specialist authorized.
- **National fine-height volume = product constraint.** ~18.25 TB compressed / 55.23 TB raw projected; mandatory cook/
  storage/replication/recook budgeting before national activation.
- **Inherited research reports contain overclaims + a citation conflation** (r1.md DDNM/MultiDiffusion/Guérin transfer;
  r2.md over-recommends Guérin 2016 + misidentifies Argudo 2017 as the orthophoto FCN — actually Argudo 2018 CGF 37(2)
  DOI 10.1111/cgf.13345). Source ledger must expose these.
- **Erodible-slope A/C/B condition bundle = R0-preflight ready, NOT recipe/preview ready.** Final condition artifact
  `10a5ea53b007...`. Dev A + Dev C runnable; Dev B strict-abstention (null Huumus unknown ≠ absent). `ready_for_r0_input_freeze`
  true, `ready_for_recipe_freeze_or_preview` FALSE. Blocker = complete recipe freeze + frozen run before **sealed OOD2
  `9688702` (chunk `(2441,1508)`, bounds `[681088,6442368,681216,6442496]`) may be opened**. Orajõgi `9688685` consumed/
  disqualified. Dev A/C/OOD2 on sheet 54481; Dev B on 54472.
- **Second erodible-slope Development role VACANT.** Dev A is the sole runnable morphology-development site (positive
  control). Candidate `90df9d5c...` rejected (line protected, 0/122 form-active). Local metadata candidates ETAK
  `9688758`, `1812065`, `1812320`, `1812321` all fail contact/area/span gates. Correlated Dev A east/west REJECTED
  (`r0_correlated_within_site_two_crop_visual_rejected`, comb-like non-branching morphology). Domain closure
  solver-authorized at +256 m (manifest `c2ea6bf8...`) — valid ONLY for Dev A west/east, no independent-site credit.
- **Deterministic branch-initiation competition PARKED** after 2 bounded rejections (any-positive D∞ over-connects sheet
  flow; dominant-flux removes comb but no recognizable morphology). Reopen ONLY with a structurally different initiation
  model + new measured evidence; NOT for score/adjacency/threshold variants.
- **Agriculture + combined rill route PARKED** after R0 morphology failure. Hinsberger (`dec74017...`) + Alessio
  (`a3a4835b...`) both `research_diagnostic_only`/`not_authorized` (unresolved units/CRS/registration/masks). Agriculture
  is NOT the first browser showcase; do not let sunk work trigger a default agriculture preregistration.
- **Raised-bog R0 = active visually-structural route, generation NOT authorized.** Preregistration
  `blocked_before_r0_generation`. V1 whole-form capacity rejected (1 hollow/1 group vs 200/6-group). V2 plurigaussian
  rejected (`038e3695...`: 621 lawn, 1 hollow, 0 hummocks; all 8 folds fail). Condition snapshot `8d450c7f...` FAILS
  CLOSED — accepted C0 lacks selected site LOD0 `(0,81,89)`, all 65,536 halo cells height-unknown. Dev mire
  `etak-component-0004069028`; sealed abstention mire `etak-component-0004069025`. Pure raised-bog blank `Huumus` is a
  documented non-applicable horizon (T-only profiles) — NOT for mixed/non-bog components.
- **Erodible-slope `fluvial.rill_gully_seep_spring` = selected next browser regime** (NOT `outcrop.sandstone`).
  Heightfield-compatible incision/headcuts/seep/toes only; vertical wall/caves/undercuts/talus/water = hard exclusions.
  Many process specialists PARKED (morphodynamics `c7deecd2...`, multiscale-amplification `0a150a35...`, headcut-event
  `0ecf3595...`) — each failed the visible gate (combs/hairlines/near-parallel). Next challenger must be materially
  different whole-domain headcut/gully + depositional-toe hypothesis; exhausted scoring/adjacency/threshold variants forbidden.
- **FOREST is the accepted positive.** `forest.mesic_mineral` = LOCKED research owner, `research_preview_investigated`,
  via irregular Wendland-C2 transport over measured K11/K32/K36 LUKE forms. Investigated tiers: single full-parent (recipe
  `6d28b79a393f...`, manifest `48bacfc559d1c5f626168d753d4ef3ec46394847ef2d6ad5f3894936641b540d`, frame 158, user 2026-07-15);
  three disjoint sites (`dffa2ca2...`/`8459fa8f...`, blind user pass); adjacent-parent continuity (`d1cdf076...`/`b97c88a6...`);
  four-parent junction 1024×1024 (`dc4c2861...`/`7563ea22...`). NARROW status: no production/`latest`/national/beyond-tested/
  other-forest/non-forest authority. Runtime radius: LOD-2 full to 32 m fades by 40 m, LOD-1 to 128 m fades by 160 m —
  review links start inside an eligible patch, never call the whole km finest; keep WebGPU boots ISOLATED (2 concurrent =
  transient `null.constructor` fatal).
- **Full-LOD0 forest scale-out packed + verified; final visual review OPEN.** Artifact `6d3bae2f...` (32768×32768),
  recipe `e9b45446...`, manifest `49d298c4...` (256 LOD-2, 16 LOD-1, 1,001,639,616 B). 7 gates pass, zero seams. First
  packing had a shared-parent quantization-datum defect (superseded; re-encoded under one shared datum). 3/4 WebGPU
  positions pass (94/93/96); **strong-morphology boot + full-extent user repetition/transition/grounding review remain OPEN.**
- **Soil conditioning discards structure.** `process/soil.py` keeps only first component/simplified texture. Full soil
  profiles + mapped cliff/shore constraints must be first-class cook inputs. Rock/cliff regimes require typed macro
  reconstruction of shoulders/edges/ledges/scarps BEFORE fine morphology; adding bumpiness to a coarse ramp fails.
- **EGT 1:50k geology is NOT national coverage** — Taevaskoda unmapped (nearest polygon ~7.05 km). Preserve coverage/
  confidence mask, fall back to 1:200k prior; missing 1:50k never becomes an implicit class. EGT 1:200k code-domain gap
  closed (commit `7ca8f4b`, bundle `77b81b2d...`); `Q_Litoloogia_200`/`Q_Genees_200` distinct from 1:50k — no substitution.
- **All escarpment / cliff / rock reconstruction routes PARKED, rock/cliff regimes ABSTAINED.** Exhausted (each 2-attempt):
  station-wise PCHIP, global-2D variational, ALS/TGV macro specialist (`a5d101c1...`, packed `bbba982d...`, then
  `research_rejected`; only 27.4% active solve, ALS on 6.4% of nodes, 99.99963% veg/shadow-masked), robust-ALS multiscale,
  Biała whole-field exemplar, Biała paired-sparse dictionary (checker/waffle phase), single-field learned (texture not
  organization), domain diffusion (`597b54f6...`; evidence-capacity ceiling ~4 direct 128 m windows), second Biała epoch
  (+coverage not diversity), Biała Góra 2D capacity (representation not transfer), glaciolacustrine-sand graph,
  curvilinear/injective sand-bank (folds/soft shelf). Nine other Biała epochs unfetched (27 GB, unauthorized). Resume ONLY
  with a materially different continuous 2D structural authority + new frozen 2-attempt authorization — NOT by relaxing
  envelopes/tapers/weights. Taevaskoda `(-1,607,372)` never opened = untouched blind sentinel.
- **Multi-form mapped-rock/till: bounded positive, narrowly capped.** Family-4 tiny-stone socket ACCEPTED as a bounded
  checkpoint (additive `C1=C0+forest+rock delta`; user saw a slight hump appropriate at scale). Rock-only v4 adds nothing;
  Family 2 PARKED. NOT a boulder-scale/rough-till/production/national owner. Forest+boulder replacement REJECTED (rectangular
  forest-detail loss = exclusive-occupancy bug in `boulder_composition.py`). Rough-till quilting parked. Forest+mapped-boulder
  transition blocked by site selection (rock 131-323 m from eligible forest) — resume needs a rock↔forest overlap gate.
- **Ahja river bumps** — corrected in `009fd...`. False TIN bridge (~225 m², 52.22 m, 13.45 m into channel near
  `E679788.35,N6444811.45`). National flowing-water activation BLOCKED: Jägala waterfall is a blind protected-feature
  regression case; Ahja stations 40-70 fail observation continuity (fit downstream from station 71, abstain elsewhere);
  ETAK footbridge `4477659` at station 386.497 needs an executable occlusion footprint before national. Repair must not
  erase hydraulic discontinuities (split graph at protected jumps).
- **Optional coarse geology: functionally accepted, NOT performance-qualified.** Commits `30a16ef`/`bf0b68b`, manifest
  `ce4cf01c...`, boots 88/70 (on) + 90 (off). Appearance-only, zero geometry/morphology authority, old manifests must
  stay exact no-ops. Fresh game-only GPU trace + perf acceptance REQUIRED before broad adoption (does not block asset-gen).
- **Raised-bog measured passes are carriers, not synthesis.** Valgesoo (`6e0148da...`) = gentle broad carrier, no
  hummock-lawn-hollow. Process v3 (`f6bf62ee...`) = subcell labyrinth (6,725 hollows/7,533 hummocks). Form-scale graph
  (`caf9e5f2...`) = periodic corrugations. All negative evidence; do not tune/pack.
- **Mrzezyno dune** = 1-8 m dune-landform scale prior ONLY (target `f6aa61f6...`); no fine-ripple/beach/Estonia-owner.
  First Estonia dune attempt (`4edad71d...`) parked (structural authority conflict — exact 1 m block-mean preservation
  removes the only authorized dune relief).
- **Integration risks**: root `estonia-asset-gen` worktree is dirty w/ old-agent changes — MUST NOT copy the obsolete
  128 m global-grid rewrite or hash/noise synthesizer. Pilot preview may materialize locally but must not update
  `latest.json` until realism/masks/user acceptance pass. Commit `8bded0a` closed the generic packed-surface grounding
  correction (trees/boulders/understory/debris) w/o synthesis/wire change; full-L0 forest user review still confirms the combined result.

---

## Part C — Orchestrator watchlist (highest-leverage don't-forget)

1. **Year is 2026, not 2025.** Latest user decisions: 2026-07-16 (forest escalation), 2026-07-15 (Stage 2E, full-parent
   forest acceptance). Today per env: 2026-07-17.
2. **Sealed holdouts — NEVER open before their frozen recipe**: `JS_SPRUCE1` (Järvselja forest blind); erodible-slope
   **OOD2 = ETAK `9688702`**, chunk `(2441,1508)`, bounds `[681088,6442368,681216,6442496]`; raised-bog sealed abstention
   mire `etak-component-0004069025`; Guérin **k19** (sealed until numeric gates + human receipt); Taevaskoda parent
   `(-1,607,372)` (escarpment blind sentinel).
3. **CONSUMED / disqualified — no future untouched-test credit**: Orajõgi `9688685` (consumed/disqualified); Dev B ETAK
   `1826691` (rejected strict-abstention); Orajõgi/Dev C crop `(2438,1519)`; ForestSemantic `test1`/`test2` (consumed —
   never retune against them); Guérin **observer-blind holdout claim permanently invalidated** (a generic NPZ inventory
   opened k19 arrays) — Guérin/LUKE establishes NO algorithm-capacity result.
4. **Two-attempt park rule**: every escarpment/cliff/rock/erosion family gets ONE run + at most one consolidated
   correction; on failure, focus MOVES — do NOT resume by tuning weights/tapers/thresholds/patch-size/overlap/iterations.
   Resume only with a materially different mechanism + new frozen authorization.
5. **A disappointing result triggers a PREMISE audit, not method-varying.** Go up a level (params? metric? the setup one
   level up?) before declaring anything not-doable. Filed-under-"given" is the prime suspect.
6. **NEVER pack / NEVER `latest`**: any research preview; any candidate not in `research_preview_candidate`; agriculture
   (until a separate reviewed agriculture preregistration); any parked negative artifact; Moore B1-only output; a research
   manifest through the atomic `latest` path (release tooling must hard-reject).
7. **`forest.mesic_mineral` is the ONLY locked morphology owner** (research, not production). Its authority is narrow:
   no production/`latest`/national/other-forest/non-forest. Do not generalize it silently.
8. **Full-LOD0 forest (`6d3bae2f...`/`49d298c4...`) still has an OPEN gate** — strong-morphology boot + full-extent user
   review. Do not treat it as accepted.
9. **Catalog pin `estonia-microtopography-phenomena/2026-07-13.1`** (SHA `6ca03d5b...`): any phenomenon/eligibility/scale
   change forks the catalog and invalidates rows. Registry = 26 rows, 25 unsupported + peat.raised_bog research.
10. **`STAGE1-EXECUTABLE-METHOD-CONTRACT.md` is superseded — do NOT implement.** Old `release.py` (fixed old-base SHA,
    17 keys, no-pilot-to-`latest`) must be REPLACED not bypassed.
11. **Taevaskoda is a coordinate, never a special case.** No Taevaskoda-specific constants/masks/tuning; general fixes
    apply to the same physical regime everywhere. It supplies NO parameter/threshold/candidate-selection input.
12. **`micro_synth.py` must be deleted — no fallback.** No runtime synthesis/shader/format change except the §19.5
    grounding repair and the §4.5 geology appearance path.
13. **Every unsupported regime ABSTAINS** — no generic filler, no nearest-regime fallback. `unknown → abstain`.
14. **Paid acquisition / side projects stay REJECTED** unless the user explicitly reopens budget. Do not quietly restore.
15. **Keep WebGPU boots isolated** (concurrent boot-smoke = transient `null.constructor` fatal); state the finest-detail
    radius in every review link.

---

## Part D — Structurally questionable / self-contradictory (engineering opinion, not authority)

1. **9.11.1 escalation contradicts 9.11's own conclusion.** The forest sparse screen is the path KNOWN-ISSUES says
   permanently invalidated its blind-holdout claim and "establishes no algorithm-capacity result" — yet its carrier is
   packed, previewed, and becomes the locked owner. The "generally should transfer" relaxation is doing all the work.
2. **The k19 human-receipt gate (9.11) is a social gate dressed as technical** — admittedly "not cryptographic proof of
   identity," a replayable schema anyone with repo access can fill, yet the whole seal rests on it.
3. **The B2 four-phase gate (12.7.1) spends a dozen hard thresholds** qualifying a source the same section already
   declares `target_evidence_insufficient` and says can never rank a winner — high complexity for a non-authorizing path.
4. **§17 budget numbers are a lower bound presented as a plan.** The whole apparatus is pinned to the rejected-LUKE ratio
   while accepted morphology is expected to "compress worse," so every headline TB figure understates.
5. **Two-attempt park discipline is under-powered for cliffs.** A dozen families each died after 1 run + 1 fix on the same
   failure (soft macro / phase-lock / texture-not-organization) that §domain-diffusion diagnoses as an evidence-capacity
   ceiling (~4 direct 128 m windows). The spec keeps spending method-attempts on what it calls a data problem.
6. **§10.4 hardcodes exact transient ranges (45/777/897, specific cx/cz)** as "authorization constraints." These are one
   Ahja capture; §14.3 admits multi-parent needs generalized tooling, so they invalidate on the next reach.
7. **§13.1 forbids two specialists on same phenomenon+scale, but §11.1 has transition rows** with "non-overlapping
   ownership." The legal-transition vs illegal-double-claim boundary is asserted, not operationalized.
8. **§16.6's binomial (`P=0.01953125`) is decorative** — a single user veto overrides nine independent reviewers, so the
   statistical apparatus and the actual decision rule differ.
9. **KNOWN-ISSUES (~2160 lines) has poor open/closed separation** — live state (forest locked, full-LOD0 open, OOD2
   sealed) is buried in terminal parks; high risk of acting on a closed item. (This digest exists because it doesn't scan.)
10. **"Abstain everywhere unsupported" is in tension with a shippable product.** 25/26 regimes abstain, cliffs
    indefinitely; the actual deliverable is corrected structure + one foreign-analogue forest carrier, so the DoD ("finest
    surface visibly adds realistic geometry") is currently unreachable nationally under the spec's own evidence rules.
