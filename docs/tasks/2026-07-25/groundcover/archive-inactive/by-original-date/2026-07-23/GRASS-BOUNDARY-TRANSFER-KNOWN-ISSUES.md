# Ground-cover box-boundary transfer: known issues and resume record

**Date:** 2026-07-23
**Status:** prior finite codecs parked; GBC2-K6 offline gate active; runtime
still held

## Requested outcome actually usable now

No new production ground-cover renderer is usable. The exact continuous
periodic point-ray factorization is reusable, but its finite physically
filtered codec and finite Estonia patch composition have not passed.

## Effort already spent

- Derived guarded common-slab entry and four-dimensional whole-forward
  transfer for arbitrary finite triangle soup.
- Closed poles, copies, Tier-1 affine layers, joint multi-species ownership,
  terrain/deformation bounds, moss lane choices, and the finite-mask side
  first-passage equation.
- Confirmed the periodic point-ray identity on 192/192 accepted-source rays.
- Ran one categorical codec census, one shared-origin diagnostic cook, and one
  production-pixel premise correction. No runtime/shader work was performed.

## Reusable result

- `GRASS-BOX-BOUNDARY-TRANSFER-MATH.md`: exact central-ray reduction and the
  standoff/shear impossibility theorem.
- `GRASS-BOX-TERRAIN-MOTION-CLOSURE.md`: finite ecological-patch side entry,
  byte law, terrain/motion contract, and first-entry-closure certificate.
- Both offline harnesses and content-addressed artifacts, including the
  validity addendum which prevents the invalid RED from being reused.

## Exact blocker

The product needs a finite, directly indexed approximation to a
standoff-dependent 2D pixel integral through a 4D boundary light field. The
tested codec has no valid full-domain quality result. The last harness still
has six binding setup defects: finite-side phase/horizon mismatch,
chart-specific wrapping, oracle rather than analytic Jacobian selection,
unpacked centroids, train/test leakage with no excess temporal metric, and
incomplete total-depth moments. The actual Estonia control field has also not
passed the finite side-page and first-entry-closure gate.

## Why focus moved

Two end-to-end codec attempts failed validity. Continuing to tune samples or
VQ parameters would violate the track limit and could only manufacture another
unsupported result. Runtime transcription would preserve none of the claimed
quality guarantees.

## Fallback while the replacement gate runs

Keep the existing accepted grass runtime unchanged. Preserve the new
mathematics and offline evidence only; do not add a box carrier, floating
plane, view candidates, march, traversal, candidate list, per-species query,
or new grass geometry.

## Objective resume condition for runtime implementation

Resume only as one coherent full-domain cook that, before shader work:

1. fixes every validity defect listed above;
2. converges source truth under the exact production pinhole kernel;
3. fits physically packed records on a training domain and passes disjoint
   held-out cameras, addresses, standoffs, seams and translations;
4. validates finite Estonia side entry and no-missed-later-patch closure;
5. reports total resident/working bytes `<=250 MiB` and the declared GBC2-K6
   worst fixed decode `<=15` logical reads, together with actual anisotropic
   sampler traffic; and
6. passes RGB/coverage/silhouette/depth/normal/temporal thresholds.

Only that GREEN authorizes reading the runtime shaders as implementation
targets and transcribing the model.

The active replacement is specified in
`GRASS-RAY-SPACE-CUT-CELL-CODEC.md`. It does not tune either invalid prior
codec: it replaces point-value interpolation with certified analytic binary
visibility cuts and delegates only irreducible multiway cells to a fixed
six-pair-plane physical-pixel terminal.
