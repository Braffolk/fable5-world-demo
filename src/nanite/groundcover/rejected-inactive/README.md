# Rejected and disconnected runtime prototypes

**No source below this directory is reachable from the application runtime.** It is excluded from
TypeScript production builds and retained only so the failed boundary/GBC2/GBR4 work is not lost.

The live GBR4-v4 and carrier-closure files outside this directory are validation loaders only.
They do not bind a boundary-transfer renderer.
