# Rejected/inactive: boundary transfer

The boundary-transfer model and filtered codec did not produce a valid production representation.
The code is disconnected from the runtime and preserved solely for analysis/reproduction.
