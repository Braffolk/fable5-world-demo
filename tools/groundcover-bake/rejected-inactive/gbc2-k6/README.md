# Rejected/inactive: GBC2/K6

All tested K6 cap fits were RED. These cooks and trainers must not publish assets into the runtime
asset directory. Historical outputs belong under ignored `data/work/`.
